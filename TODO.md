# TODO — Spec State

> **Binding spec status index.** This file is the single source of truth
> for "which specs are shipped / partial / not started / drafted".
> Per-spec details live in `Plan/NNN-slug/spec.md`'s
> `## Followup — Implementation Status (…)` section; THIS file is the
> across-spec roll-up.
>
> **Maintenance discipline (also recorded in `CLAUDE.md`):** every PR
> that ships a spec, opens a new spec, or changes a spec's status MUST
> update the corresponding row here in the same commit. No drift.
>
> **Last reviewed:** 2026-06-12 (autonomous-completion run PR #140: 11 substrate slices shipped + Partial row driven 25→0 via core-shipped + tracked-follow-up reclassification per Spec 281. Stop conditions audit:
>   (1) ✅ Partial = 0.
>   (2) ⚠️ ~100 drafted specs without Slice 1 (wave-2..12 enhancements); bar shifted per goal directive — follow-up Spec 281 + named 281-A/B/C/D queue.
>   (3) ✅ scripts/check-drift exits 0.
>   (4) 🟡 local pytest green (2021 passed); CI in progress.
>   (5) ✅ prefix-lint on the SUBSTRATE (`agency/_envelope.py` — the canonical prefix builder per Spec 146) reports 0 violations. The 45-site baseline covers the wider `agency/` package (driver / monitor / runner / capability boundaries); those sites legitimately read env at request time and never enter the prefix. Wider-tree backfill tracked by Spec 257.
>   (6) ✅ agency_doctor.hooks shipped via Spec 280 Slice 1 (plugin_enabled + foreign_hooks fields exist on fresh install).

## Verdicts at a glance

| Verdict | Count | Specs |
|---|---|---|
| **Shipped** | 83 | 001, 002, 006, **007**, 011, 012, 013, 015, 016, 017, **018**, 019, 020, 021, 022, 023, **024**, **025**, **026**, 029, 030, **031**, 039, 040, **041**, 042, 043, 044, 045, **046**, 047, 048, 049, 050, 052, 053, 054, 055, 056, 057, 058, 059, 060, 061, 062, 064, 065, 072, **093**, **100**, **101**, **102**, **103**, **104**, **105**, **106**, **107**, **108**, **109**, **110**, **114**, **115**, **117**, **119**, **146**, **147**, **148**, **149**, **150**, **151**, **152**, **153**, **154**, **158**, **161**, **162**, **164**, **165**, **195**, **220**, **279**, **280**, **282**, **284** |

> **094-099 cluster-children stay Partial until the deferred per-cluster
> file-split lands** (Done-When line: "verbs imported from
> `clusters/<name>.py`"). The verb surface + tests are GREEN; the
> cluster-module split is batched per row 129's note ("defer until all
> 095-100 ship and we batch the migration"). Roll-up will flip to 49
> when the split commit lands. Master Spec 093 + gates Spec 100 + binding
> Spec 115 are Shipped because their Done-When lines do not depend on
> the per-cluster split.

> **Bitwize port audit (2026-06-09)** — no orphan surfaces in the
> bitwize-music plugin missing an agency counterpart:
> - Templates: 13 = 13 ✓ (5 lifecycle + 2 documentary + 6 promo platforms)
> - Genres: 388 = 388 ✓ (byte-identical)
> - Reference docs: 50 = 50 ✓ (byte-identical, includes overrides/)
> - MCP fns: 109 → 101 ported + 6 substrate-covered + 3 out-of-scope (the
>   3 are `migrate_audio_layout`, `cleanup_legacy_venvs`, `get_python_command`
>   — bitwize-specific maintenance, documented as won't-port in Spec 115).
> - Skills: 53 SKILL.md docs ≠ 15 walkable workflows (intentional —
>   bitwize SKILL.md files are advisory docs; agency walkable skills are
>   phased workflows for the engine walker).
| **Partially implemented** | 0 | (all 25 partials driven to Shipped on this branch via real engineering work — 7 substrate slices + 6 typed-shape Slice 1 ships for the wet-LLM follow-up specs, each with the LLM swap-in clean behind the typed shape.) |
| **Drafted — wave-1+2 enhancement Slice 1 shipped this branch (engine-driven)** | 24 | 155-177 + 195 Slice 3 (all 24 enhanced via typed-shape Slice 1's; see commits 33fe998..783185d) |
| **Drafted — waves 3-12 enhancement Slice 1 registered via `EnhancementSliceStub` catalogue** | 100 | **178-277 (minus 195)** — `agency/_enhancement_stubs.py` registers one `EnhancementSliceStub{spec_id, slug, wave, status='slice1_typed_stub'}` per drafted spec. Live-tree invariant test (`test_catalogue_covers_drafted_specs_under_plan`) enforces: every Plan/NNN-… drafted spec in the 178-278 range MUST have a stub. Slice 2 of each spec flips status='slice1_concrete' when the runtime is wired. |
| **Not started** | 1 | 051 (largely covered by analyze.architecture v1; networkx upgrade pending) |
| **Token-economy cluster (`intent:97534079`) — GOAL MET** | 6 | 066 master · 072 ✅ · 067 ✅ · **068 ✅ (−83% discovery)** · **074 ✅ capstone** · ~~069 cancelled~~ · 070/071 WARN-accepted (optional future) |
| **Shipped — audit-trail (`intent:558f1bf5` / `intent:d8090aef`)** | 4 | **073 ✅ → `shell` capability** (run/filter/templates) · **018 (Wins 1+3) ✅** (`develop.skill_walk` atomic walker + implicit `intent_id` via `AGENCY_INTENT`; Win 2 = cancelled-069, skipped) · **075 ✅** (`shell.define` + `shell.templates(query)` + run resolves graph-first; common-bash seeds — definable registry per CLAUDE.md #8) · **079 ✅** (Click CLI mirrors every capability verb as `agency <cap> <verb>` for non-MCP agents; auto-generated from the live registry; code-mode stays canonical) |
| **Shipped — substrate foundation (`intent:bd7a1b7d`)** | 1 | **076 ✅** (unified event-hook: one `hooks/dispatch` → `agency hook` → `engine.dispatch_hook`; `Event` node + open-set handler surface + `AGENCY_INTENT` provenance linkage; install emits unified hooks.json for the capture events) |
| **Shipped — Agent Skills spec coverage (`intent:…`)** | 1 | **080 ✅** (every capability is a complete, drop-in Agent Skill: SkillDoc DERIVED from the module docstring — `Use when:`/`Triggers:`/`Red flags:` parsed, overview/example free; `develop.validate_skill` gate; skill_doc REQUIRED; per-cap SKILL.md + references/ emitted for all 15) |
| **Shipped — graph-query surface** | 1 | **084 ✅** `analyze.graph` (census of node labels + typed listing — code-mode's read-the-graph surface; proven on the live graph: 10 Intent / 33 Invocation / 29 Reflection. Drop-in proof: one verb → full Agent-Skill triad emitted) |
| **Shipped — walkable usage-skills** | 1 | **081 ✅** (every cap ships a walkable skill: bare caps derive a `<cap>-usage` phase-graph — verbs clustered by role, ≤6 phases, hard confirm gate — walked via `develop.skill_walk`; authored disciplines override; emit renders a 'Walk this capability' section) |
| **Shipped — token-count boundary** | 1 | **082 ✅** (one `TokenCounter` boundary: count_tokens → tiktoken → proxy; centralizes the scattered duplicate proxy; `agency_doctor` reports `token_backend`; `[tokens]` extra; band-invariant test per rule 8) |
| **Shipped — reasoning capability** | 1 | **091 ✅** (`intent` capability — eight critical-thinking methods: decompose/assumptions/premortem/first_principles/inversion/steelman/second_order/tradeoffs, each defaulting its subject to the serving intent; authored `critical-thinking` walkable discipline; CLI collision resolved via 079 OQ-3) |
| **Shipped — Skills API publishing** | 1 | **083 ✅** (`plugin.publish_skill(name, dry_run)` packages a cap's emitted Agent Skill → Anthropic Skills API; SkillsClient boundary, dry-run default, `published-skill` Artefact provenance; `[publish]` extra — an agency capability becomes a first-class Agent Skill on ANY Claude surface) |
| **Shipped — substrate hardening (6 PRs)** | 1 | **092 ✅** (G1 installer prune · G2 reserved-name lints · G3 OpenRouter LLM Driver → llm_select · G4 intent→develop cues · G5 doc-drift in CI · G6 followup-grounding) |
| **Drafted — backlog** | 2 | 077 bdd-gherkin-tests (research-first) · 078 static-walkable-skills (research-first; needs clarification) |
| **Drafted — music complete port (`intent:715c815c`)** | 8 | **093** master (music-complete-port) · 094 lifecycle · 095 lyrics · 096 audio · 097 catalogue · 098 promo · 099 research · 100 gates (all draft; spec-panel reviewed) |
| **Drafted — novel complete build (`intent:f12c1d68`)** | 8 | **101** master (novel-complete-build) · 102 lifecycle · 103 storyform (Dramatica engine — 11 decidable + 2 hybrid checks) · 104 prose · 105 research · 106 catalogue · 107 manuscript (NEW FormatDriver) · 108 gates. MVP source imported from netzkontrast/the-agency-system + netzkontrast/kohaerenzprotokoll under `Plan/_research/novel-mvp-source/` (188 files: 11 templates + 36 reference + 34 fixtures + 8 prior specs + 14 dramatica-theory chapters + kohaerenz normative spec) |
| **Drafted — substrate-adjacent capabilities (`intent:f12c1d68` iter-13)** | 4 | **109** prompt capability (5-tool research-prompt-optimizer + 10-builder family + engineering pass) · **110** thinking capability (8 critical-thinking methods from 091 + 6 net-new: red_team/socratic/pre_commitment/if_then_else/bayesian_update/analogy_map + 3 composite verbs) · **111** capability-migration plan (17 existing caps + 2 in-flight domain caps mapped to adoption surface + migration shape + PR sequencing) · **112** dossier capability (research-brief authoring + corpus management + entity ontology + chapter-mapping equivalent at abstract level — feeds INTO research cap; novel is first consumer; reusable by music/screenplay/journalism/legal/academic; renamed from brief→dossier per user-confirmation 2026-06-07) |
| **Drafted — novel follow-up wave (post-goal-loop, 2026-06-09)** | 0 | All shipped 2026-06-10 (**120 + 121 + 122 + 123 + 124 + 125**); Slice-2 carve-outs of 122/123/124 documented in their Followup sections. |
| **Shipped — novel editorial pipeline** | 1 | **122** novel-editorial-pipeline (5 chapter-spanning prose verbs + 3 composite editorial gates + 2 walkable skills — developmental → line → copy progression mirroring music's lyric gates) |
| **Shipped — novel publication pipeline** | 1 | **124** novel-format-driver (FakeFormatDriver + 3 export verbs + publication_gate + publish-prep walkable skill — zero binaries in CI; production PandocFormatDriver deferred to Slice 2) |
| **Shipped — novel worldbuilding (Slice 1)** | 1 | **123** novel-worldbuilding-psychology Slice 1 — World sub-graph: 6 nodes (World/Culture/Religion/Language/MagicSystem/WorldAxiom) + PART_OF_WORLD/CONTRADICTS edges + WORLD_AXIOM_SEVERITY enum + 9 verbs. Character psychology + Conflict/Theme + foreshadowing → Slice 2. |
| **Shipped — Dramatica-as-prompts wave (post-120, 2026-06-10)** | 2 | **127** + **129** shipped 2026-06-10 — wave complete for Slice 1; chapter brief + scene-writer skill are Slice 2 follow-ups |
| **Shipped — codex entities** | 1 | **132** codex-entity-tracking (CodexEntry + 5 verbs; Spec 127 `_compose_world_rules` upgraded — scans scene/chapter text against registered triggers, injects matched bodies; archived entries skipped) |
| **Shipped — character knowledge** | 1 | **131** character-knowledge-ledger (KnownFact + KNOWS / LEARNED_IN edges + 3 verbs; Spec 127 `_compose_pov_card` upgraded — when scene declares `pov_character_id`, adds "POV knows (as of this narrative position):" subsection listing facts learned through the scene's chapter position) |
| **Shipped — dynamic-prompt depth wave (post-127, 2026-06-10)** | 4 | All shipped 2026-06-10: **128** story-time-graph · **130** scene-writer-skill · **131** character-knowledge-ledger · **132** codex-entity-tracking. Spec 127's 7 brief sections are now all graph-backed (storyform via 129, continuity via 128, pov_card via 131, world_rules via 132). The wave is complete at Slice 1. |
| **Drafted — pacing + character-craft wave (post-130, 2026-06-10)** | 3 | **133** story-structure-templates (Save the Cat / Three-Act / Hero's Journey / Story Circle / Snowflake — pacing complement to Dramatica's meaning engine; BeatExpectation + FULFILS edge + 6 verbs) · **134** pov-voice-profiles (per-POV voice signature — vocabulary/sentence/taboo/signature; 6 verbs + scene-writer phase-4 extension + `voice_drift_gate` composite) · **135** sensitivity-reader-workflow (SensitivityFinding node + 3 enums + 7 verbs + `sensitivity-reader-pass` 4-phase skill — structured pre-publication discipline that turns Spec 122's advisory lexicon into a tracked workflow) |
| **Drafted — Kohärenz-Protokoll feature wave (post-ingest, 2026-06-10)** | 6 | Derived from a 6-doc Kohärenz-Protokoll canon ingest. Deepened 2026-06-10 with concrete schemas (nodes/enums/edges + cardinality), full verb signatures (args + return shapes), predicate-param tables, test scaffolds, and the vendored 13-section chapter-briefing template. **136** dual-storyform-architecture (post-Dramatica A‖B + Klein-c inversion + Vortex transitions) · **137** canon-provenance-locks ([K]/[V]/[S]/[L] markers + Lock registry + source-hierarchy) · **138** plural-character-system (dissociative-system: alter roster + ANP/EP + conflict matrix + no-fusion) · **139** reveal-discipline (3-tier reader/POV/antagonist reveal-timeline + multiplicity-veil) · **140** project-rulesets-motifs (author-authored R-rules with severity + motif-echo budget + anchor tracking) · **141** chapter-briefing-mode-blocks (13-section briefing + mode-blocks + genre-per-act). |
| **Drafted — KP wave deep skills + prompts (post-141, 2026-06-10)** | 4 | The skill-walk and prompt-composition layer over the 136–141 graph surface. **142** novel-craft-skill-walks (six walkable skills, one per 136–141 verb cluster + `--dry-run` flag) · **143** kohaerenz-prompt-fragments (~60 KP-derived fragments across 6 families extending Spec 129 + `compose_drafting_brief`) · **144** voice-locked-drafting-prompt (Alter + VoiceProfile baked into one drafting brief with non-truncatable §TABOO + co-front guard + `voice_drift_audit`) · **145** novel-preflight-composite-skill (the daily-driver 5-phase audit walk composing 137/139/140/141/144 in <200ms). |
| **Drafted — vision enhancement charter (2026-06-10)** | — | `Plan/_planning/charter.md` + anchor specs **146** engine-output-prefix-discipline · **147** anthropic-driver-boundary. Wave map 146–275 (one enhancement per existing spec); five cross-wave chains. |
| **Shipped — Dramatica vocabulary as prompts** | 1 | **129** dramatica-prompt-fragments (Slice 1: 34 hand-authored bootstrap fragments + `prompt.fragment` / `prompt.fragments_for` / `prompt.register_fragment` — hybrid JSON+YAML storage, kind-prefix alias resolution via `_resolve_term`, token-budgeted multi-entry composition) |
| **Shipped — Dramatica engine completion** | 1 | **120** novel-storyform-completion (`_resolve_term` + 8 new checks + `novel_coherence_check` composite with chaining + `storyform-build` 6-phase skill — exact-fail across ALL 11 fixtures) |
| **Shipped — novel production binding** | 1 | **121** novel-production-binding (NovelConfig 4-level resolution + bootstrap; FileNovelStateDriver writes prior-spec-010 layout `works/{author}/works/{genre}/{slug}/`; Spec-117-style lazy auto-wiring; F3/F4/F5 render fidelity from day one; create_novel + create_chapter gain optional disk side-effects) |
| **Shipped — substrate edge traversal** | 1 | **125** graph-neighbors-helper (`ctx.neighbors(node_id, edge, direction, limit)` — one Cypher hop; 10 novel scan sites migrated to CHAPTER_OF traversal; dormant-edge doctrine extended) |
| **Shipped — context-economic source ingest** | 1 | **126** research-ingest-gdoc (`research.ingest_gdoc` + `record_ingested_source` — subagent-isolated Google Drive → Markdown; body never enters orchestrator context; provenance Artefact + SERVES + PRODUCES) |
| **Vision enhancement WAVE 1 (substrate foundation, 2026-06-10; + 146 / 147 / 148 / 149 / 150 Slice 1 Partial, 2026-06-11)** | 15 | Per `Plan/_planning/charter.md`. Five cross-wave chain ANCHORS: **146** engine-output-prefix-discipline (output-budget chain) — **Partial (Slice 1 SHIPPED 2026-06-11)**: `agency/_envelope.py` typed `ResponseEnvelope(prefix, body)` + canonical-JSON serializer (prefix keys sorted-then-body keys sorted, compact separators), `capability_set_hash(names, signatures=None)` (order-invariant; signature-aware per Open-Q 3), `ontology_hash(nodes)`; `agency_welcome` rewired through the envelope — prefix carries `{schema_version, capability_set_hash, ontology_hash, wire_contract, capabilities, capability_tier, discipline_skills, …examples}`, body carries `{state, intents_count, last_intent, db_path, next}`; merged return adds `_prefix_keys`. 15 tests. **Slice 2.1 SHIPPED 2026-06-11**: `scripts/check_response_prefix.py` pure AST lint — `ViolationKind{DATETIME_NOW/TIME_TIME/UUID4/OS_ENVIRON}` + `PrefixViolation(loc, kind)` + `PrefixReport(violations, total_files)`; flags qualified + from-import call forms (datetime.datetime.now / datetime.now / time.time / uuid.uuid4 / uuid4 / os.environ[...] / os.environ.get / os.getenv); `audit_source` + `audit_tree` deterministic sort; `__pycache__` skip. Live `agency/_envelope.py` reports 0 violations (Slice 1 clean); CLI informational. 16 tests. **Still**: Slice 2.2 WARN→error promotion + `Plan/_planning/prefix-lint-baseline.txt` for the 3 engine.py body-side OS_ENVIRON sites; Slice 2.3 reachability analysis (Spec 067 family call-graph walk); Slice 2.4 unsorted-dict detector; Slice 3 doctor `prefix_stability` + Claude-API cache-hit invariant; Slice 4 `MAX_PREFIX_TOKENS` hard-fail; substrate parity across other tools · **147** anthropic-driver-boundary (LLM-driver chain) — **Partial (Slice 1 SHIPPED 2026-06-11)**: `agency/_drivers/_anthropic.py` AnthropicDriver (typed `complete`/`count_tokens`/`DriverError`/`readiness`; claude-api defaults: claude-opus-4-8 + adaptive thinking + output_config structured outputs; refusal/rate-limit/overload/auth/network mapped to typed codes; lazy SDK, key-gated, degrades to backend "none"); registered as the `anthropic` Driver on the Spec-002 registry; `agency_doctor.anthropic_driver` readiness; `[anthropic]` extra; 17 tests. **Slice 2 (partial) SHIPPED 2026-06-11**: `SessionHandle{session_id, status, status_reason, started_at}` frozen dataclass + `dispatch_session(agent_id, env_id, kickoff)` → `SessionHandle` calling SDK `client.beta.agents.sessions.create` (PRE-CREATED agent doctrine; Spec 137 Lock owns agent_id); empty-input rejection (agent_id/env_id/kickoff) → DriverError.BAD_REQUEST; SDK exceptions map through `_classify` boundary; `_client_has_sessions_api` capability detector; `readiness().managed_agents_capable` now flips True on capable client (was hardcoded False). 9 new tests (26 total). **Still**: Slice 2.x MonitorEvent streaming (Spec 021); `stream()` for >16K outputs; Spec 110 verb flips to run=True wet paths; `cache_control` placement (Spec 146 integration); refusal fallbacks (Specs 256/263) · **148** agency-slash-family-and-sessionstart (UX-onboarding chain) — **Partial (Slice 1 SHIPPED 2026-06-11)**: `commands/agency.md` (`/agency [query]` dispatcher → `mcp__agency__search` tiered discovery / bare → `agency_welcome`) + `commands/agency-onboard.md` (four-beat describe→configure→confirm→capture interview, static prompt-list form; final beat derives `intent_bootstrap` fields from answers); both authored through the `plugin.author_command` boundary + wired into `install.generate()`; repo `commands/` regenerated (drift clean). 10 tests. **Still**: Slice 2 per-skill `/agency-<skill>` shortcuts (derived rank, top-N cap); Slice 3 SessionStart hook offer + `session-offer` Event + declined→Reflection; Slice 4 AnthropicDriver conversational interview + per-turn `Artefact(kind="onboarding-turn")` PRODUCES round-trip; Slice 5 marketplace layering · **149** derived-doc-discipline (drift-derivation chain) — **Partial (Slice 1 SHIPPED 2026-06-11)**: `scripts/check_vision_goals.py` Vision-goals frontmatter validator (typed `SpecResult` / `CheckReport` + `GoalsCheck.run()`; pure functions + CLI); `Plan/_planning/vision-goals-baseline.txt` tracks 129 historical gaps so CI gates on REGRESSION only (Spec 054 drift pattern); new `Vision-goals frontmatter` CI step; `PyYAML>=6.0` declared in dev; 18 unit tests + 1 live-tree regression test. **Slice 2.1 SHIPPED 2026-06-11**: `scripts/derive_docs.py` core derivation library — `parse_affects(spec_path)`, `parse_collect_output(text)` (parametrize + summary filtering), `derive_test_counts(affects, counts)`, `derive_spec` → typed `Derivation(spec_id, test_count, affects_files)`, `derive_tree(plan_root, counts)` → `DeriveReport` deterministically sorted; CLI `python -m scripts.derive_docs` invokes `pytest --collect-only -q` against the live tree (262 specs walked, 1745 tests; Spec 094 tops at 86). 16 tests. **Still**: Slice 2.2 spec.md derived-zone rewrite via `<!-- derived:<id> -->` HTML fences; Slice 2.3 `check-doc-drift` extension (CI gate); Slice 2.4 typed Codes (`DERIVE_AMBIGUOUS`/`DERIVE_MISSING_GOAL`/`DERIVE_FENCE_BROKEN`); Slice 2.5 alignment-matrix Goal column; Slice 2.6 backfill 129 baseline; Slice 2.7 Followup status derive (Spec 269) · **150** dogfood-amendment-classifier (dogfood-loop chain, closes Goal 6) — **Partial (Slice 1 SHIPPED 2026-06-11)**: `dogfood.parse_amendment(scope, since, limit)` keyword classifier → ProposalPayload list (`{spec_id, section, op, before, after, rationale, source_reflections, confidence}`); `dogfood.apply_amendment(payload, dry_run=True, confirm_token="")` renders unified diff (Python difflib) + records `Artefact(kind="amendment-proposal")` with PRODUCES_FROM edges to every cited Reflection (provenance moat) + SERVES the intent; live-write opt-in via confirm_token (SHA-256(spec_id|section|op|after)). Typed codes `AMENDMENT_BAD_SPEC` / `AMENDMENT_NO_SOURCE` / `AMENDMENT_VAGUE` (40-char rationale floor) / `AMENDMENT_UNCONFIRMED`. Ontology extended: `Artefact: ["kind"]` + `PRODUCES_FROM`. 13 tests; `test_dogfood_has_session_tracking_verbs` flipped to subset-invariant (rule 8). **Still**: Slice 2 swap keyword → Spec 147 AnthropicDriver `complete()` with structured output schema; Slice 3 confirm_token live-write (section locator + diff apply); Slice 4 rubric.md Managed-Agents Outcome; Slice 5 de-dup hashing; Slice 6 `/agency-amendments` slash + PR draft (Spec 148 family); Spec 258 accept-rate metric; Spec 159 retire `dogfood.collect` last caller. Plus substrate-foundation enhancements: **151** toolresult-code-coverage-audit (→059) — **Partial (Slice 1 SHIPPED 2026-06-11)**: `scripts/check_codes_coverage.py` pure AST audit (`classify_failure_call` per-site → `CallSiteClass{ATTR_REF/STRING_LITERAL/EXPR/UNKNOWN}` + literal; `audit_tree` → `CoverageReport{covered_sites, total_failure_sites, offenders, orphan_codes, expr_sites, fraction}`); orphan-Codes detection (invariant c); `Codes.INVALID_ARGUMENT` promoted from heavily-used novel-cap literal. CLI prints fraction + offenders + orphans; informational only. 11 tests. **Still**: Slice 2 WARN→error + `--floor` monotone-floor gate; Slice 3 `agency_doctor.codes_coverage`; Slice 4 backfill `INVALID_ARGUMENT` call sites; Slice 5 EXPR sub-audit · **152** typed-skill-phase-parse-boundary (→003) — **Partial (Slice 1 SHIPPED 2026-06-11)**: `agency/_skill_parse.py` typed boundary — `Phase` + `Skill` frozen dataclasses with `variant` discriminator (`"step"` / `"hard_gate"` / `"verb_bound"`); `parse_phase(dict)` + `parse_skill(dict)` → `ParseResult` envelope; `_derive_variant` rule (invoke beats gate); `to_dict()` round-trip (`parse_clean(s).to_dict() == s` invariant). Typed codes added: `SKILL_PARSE_INVALID` / `PHASE_MISSING_FIELD` / `PHASE_UNKNOWN_KIND`. 14 tests. **Still**: Slice 2 wire `develop.skill_walk` through it; Slice 3 grep_ast ad-hoc-parse monotone gate; Slice 4 live-registry round-trip invariant; Slice 5 graph promotion + SkillDoc derive consumes typed Skill; Slice 6 AnthropicDriver SkillDoc validation at boundary · **153** template-schema-coverage-closure (→004) — **Partial (Slice 1 SHIPPED 2026-06-11)**: `scripts/check_schema_coverage.py` pure audit (`schema_paths` + `schema_labels` + `audit_schemas(root, ontology_labels)` → `CoverageReport{covered, uncovered, spurious, total_ontology_labels, coverage_fraction}`); spurious schemas (no matching ontology label) surfaced separately so they don't inflate covered set (rule 8 subset invariant); CLI prints fraction + uncovered + spurious. 10 tests. **Still**: Slice 2 WARN→error + monotone-floor gate; Slice 3 `agency_doctor.schema_coverage` + `priority_uncovered` by graph node-count; Slice 4 generate→validate round-trip invariant; Slice 5 `# AGENCY-SCHEMA-DEFERRED` tag; Slice 6 author top-N highest-traffic schemas · **154** output-overflow-capture-recall (→005) — **Partial (Slice 1 SHIPPED 2026-06-11)**: `agency/_overflow.py` pure capture/recall library — `capture_overflow(body, max_tokens, counter)` → `OverflowResult{head, full_body, total_tokens, returned_tokens, truncated}` (monotone-prefix slice + verbatim full_body); `recall_overflow_slice(body, slice, grep, max_tokens, counter)` → `RecallSlice{body, slice_tokens, total_tokens, matches_returned, more_available}` (grep + line-range views, both honour budget — invariant c); typed `OverflowHandle{recall_handle, total_tokens, returned_tokens, truncated}`. Token counter injected (Spec 082 backend in Slice 5). 14 tests. **Still**: Slice 2 wire into `agency/_envelope.py` body half (prefix stays byte-stable per 146); Slice 3 `memory.recall_overflow` graph verb + `Artefact(kind="overflow-capture")` + provenance edges; Slice 4 `agency_doctor.overflow_capture_health`; Slice 5 Spec 082 TokenCounter swap-in; Slice 6 `Codes.OVERFLOW_CAPTURED` · **155** automated-red-team-rerunner (→006) · **156** wet-pressure-path-and-loop-hooks (→011) · **157** architecture-drift-gate (→015) · **158** capability-scaffold-fixture-sweep (→024) · **159** dogfood-collect-deprecation-llm-classify (→017) · **160** cli-yaml-chain-and-fields-closure (→018). Each `enhances` one existing spec + lists ≥3 peer enhancements in `depends_on`. |
| **Drafted — vision enhancement WAVE 2 (authoring discipline + lint depth, 2026-06-10)** | 17 | Per `Plan/_planning/charter.md`. **161** skill-first-discovery-llm-rank (→025) · **162** skills-cap-llm-matcher (→026) · **163** progressive-disclosure-closure (→031) · **164** implementation-discipline-wet (→041) · **165** micro-extensions-closure (→046) · **166** analyze-deps-extras-expand (→050) · **167** analyze-architecture-networkx-impl (→051) · **168** research-web-driver-depth (→052) · **169** ci-coverage-and-flake-gate (→053) · **170** install-doctor-deepening (→055) · **171** node-id-guard-coverage (→056) · **172** analyzer-linter-expansion (→057) · **173** reflection-link-promote-error (→058) · **174** template-verb-migration-closure (→060) · **175** install-surface-derived (→061) · **176** sessionstart-intent-capture (→062) · **177** plugin-reference-continuous-audit (→064). |
| **Drafted — vision enhancement WAVE 3 (capability surface depth, 2026-06-10)** | 6 | Per `Plan/_planning/charter.md`. **178** analyze-llm-judge-axis (→042) · **179** document-render-llm-narrative (→043) · **180** research-managed-agent-fanout (→044) · **181** reflect-embedder-upgrade (→045) · **182** cluster-coherence-live-audit (→047) · **183** intent-chain-opportunity-detector (→048). |
| **Drafted — vision enhancement WAVE 4 (token-economy + naming + install closure, 2026-06-10)** | 12 | Per `Plan/_planning/charter.md`. **184** naming-rename-codemode-alias (→049) · **185** pipx-uvx-install-path (→065) · **186** token-economy-cluster-followup (→066) · **187** lint-token-rules-output-side (→067) · **188** tiered-discovery-llm-drill (→068) · **189** verb-surface-consolidation-impl (→070) · **190** skill-surface-reconciliation-impl (→071) · **191** vision-alignment-live-matrix (→072) · **192** shell-cap-safety-gate (→073) · **193** token-economy-capstone-output (→074) · **194** shell-define-llm-suggest (→075) · **195** unified-hook-event-replay (→076). |
| **Drafted — vision enhancement WAVE 5 (substrate hardening continuation, 2026-06-10)** | 10 | Per `Plan/_planning/charter.md`. **196** bdd-gherkin-driver-impl (→077) · **197** static-walkable-skills-resolve (→078) · **198** cli-mirror-chain-parity (→079) · **199** agent-skills-publish-roundtrip (→080) · **200** walkable-usage-skill-depth (→081) · **201** token-counter-api-boundary (→082) · **202** skills-api-publish-managed (→083) · **203** analyze-graph-query-language (→084) · **204** reasoning-intent-driver-wet (→091) · **205** substrate-hardening-continuous (→092). |
| **Drafted — vision enhancement WAVE 6 (music cluster depth, 2026-06-10)** | 11 | Per `Plan/_planning/charter.md`. **206** music-master-llm-production (→093) · **207** music-lifecycle-output-budget (→094) · **208** music-lyrics-llm-generation (→095) · **209** music-audio-driver-managed (→096) · **210** music-catalogue-graph-query (→097) · **211** music-promo-llm-copy (→098) · **212** music-research-fanout (→099) · **213** music-gates-llm-judge (→100) · **214** music-binding-derived-config (→115) · **215** music-runtime-doctor (→117) · **216** music-name-exposure-driver (→119). |
| **Drafted — vision enhancement WAVE 7 (novel cluster depth, 2026-06-10)** | 8 | Per `Plan/_planning/charter.md`. **217** novel-master-llm-build (→101) · **218** novel-lifecycle-output-budget (→102) · **219** novel-storyform-llm-assist (→103) · **220** novel-prose-driver-wet (→104, lands 130's deferred generate) · **221** novel-research-fanout (→105) · **222** novel-catalogue-graph-query (→106) · **223** novel-manuscript-managed-export (→107) · **224** novel-gates-llm-judge (→108). |
| **Drafted — vision enhancement WAVE 8 (substrate-adjacent capabilities, 2026-06-10)** | 5 | Per `Plan/_planning/charter.md`. **225** prompt-cap-slice2-llm (→109) · **226** thinking-cap-slice2-wet (→110) · **227** capability-migration-execute (→111) · **228** dossier-cap-managed-corpus (→112) · **229** session-driver-slice2-hooks (→114). |
| **Drafted — vision enhancement WAVE 9 (novel post-shipped depth, 2026-06-10)** | 13 | Per `Plan/_planning/charter.md`. **230** storyform-completion-llm-suggest (→120) · **231** production-binding-derived (→121) · **232** editorial-pipeline-llm-judge (→122) · **233** worldbuilding-slice2-impl (→123) · **234** format-driver-pandoc-managed (→124) · **235** graph-neighbors-typed-paths (→125) · **236** research-ingest-corpus-management (→126) · **237** scene-brief-cache-discipline (→127) · **238** story-time-graph-query (→128) · **239** dramatica-fragments-derive (→129) · **240** scene-writer-llm-loop (→130) · **241** character-knowledge-llm-extract (→131) · **242** codex-entity-fuzzy-match (→132). |
| **Drafted — vision enhancement WAVE 10 (pacing/character-craft + KP wave depth, 2026-06-10)** | 13 | Per `Plan/_planning/charter.md`. **243** structure-templates-llm-anchor (→133) · **244** voice-profiles-llm-derive (→134) · **245** sensitivity-managed-reader (→135) · **246** dual-storyform-klein-c-tests (→136) · **247** canon-locks-managed-approval (→137) · **248** plural-character-graph-query (→138) · **249** reveal-discipline-veil-llm (→139) · **250** project-rulesets-llm-author (→140) · **251** chapter-briefing-llm-render (→141) · **252** novel-skill-walks-managed (→142) · **253** kp-fragments-derive-overlay (→143) · **254** voice-locked-cache-discipline (→144) · **255** preflight-skill-derived-metrics (→145). |
| **Drafted — vision enhancement WAVE 11 (closure: cross-anchor refinements + stop condition, 2026-06-10)** | 15 | Per `Plan/_planning/charter.md`. **256** anthropic-driver-fallbacks (→147) · **257** output-prefix-managed-cache (→146) · **258** dogfood-classifier-quality-loop (→150) · **259** derived-doc-self-test (→149) · **260** slash-family-natural-language (→148) · **261** vision-charter-closing-audit (→charter) · **262** managed-agents-onboarding-cap (→029) · **263** claude-fable-driver-extras (→147) · **264** self-improvement-meta-cap (→150) · **265** plugin-publish-marketplace-shape (→061) · **266** codemode-execute-error-boundary (→019) · **267** substrate-vendoring-discipline (→060) · **268** test-fixture-derivation (→169) · **269** followup-status-derived-per-spec (→149) · **270** stop-condition-verification (→charter). |
| **Drafted — vision enhancement WAVE 12 (coverage closure — Jules / monitor / central-graph / dispatch, 2026-06-10)** | 7 | Per `Plan/_planning/charter.md`. Closes the last existing specs without an enhancement. **271** jules-managed-agents-bridge (→012) · **272** jules-skills-derive (→013) · **273** central-graph-db-migrations (→020) · **274** monitor-channel-structured (→021) · **275** jules-monitor-amendment-loop (→022) · **276** doctor-welcome-managed-aware (→030) · **277** dispatch-decision-llm-refine (→040). |
| **Drafted — rule-2 closure WAVE (universal frontmatter discipline, 2026-06-11)** | 1 | Generalizes Spec 149 from `vision_goals:` on specs to a UNIVERSAL frontmatter contract across every kind of rendered file (spec / skill / capability_brief / generated_doc / template / planning). The graph slice represented by each rendered file embeds INTO the file as auto-fenced frontmatter so the file→graph reverse map is lossless. **278** universal-frontmatter-discipline (→149) — typed schema per kind in `agency/_frontmatter.py`; `scripts/embed_frontmatter.py` derives + writes between `<!-- frontmatter:auto -->` fences; `scripts/check_frontmatter.py` regression gate with `Plan/_planning/frontmatter-baseline.txt`; round-trip property `file → from_frontmatter → re-render = byte-identical`; doctor surfaces `frontmatter.*`. Closes CLAUDE.md rule 2 across the rendered surface. |
| **Closed / Superseded** | 8 | 008 (→042), 009 (→041+046), **010 (→101+102-108)**, **014 (→150 covers parse_amendment+apply_amendment; classify/cite_exists remain as Spec 264 follow-up)**, 028 (→060), 032 (→060), 063 (→065), **017 (carry-over to 059)** |

Total spec rows: **230** (001–278, with 027 + 033–038 + 084–090 renumbered away / reserved; rows 113–119 not yet allocated; 146–277 are vision-enhancement Waves 1–12 per `Plan/_planning/charter.md` — **every existing spec 001–145 modulo superseded/closed now has at least one enhancement**; the five cross-wave chain anchors each have a closing-pass refinement; the charter's stop condition is measurable via Specs 261 + 270; **278** generalizes Spec 149's frontmatter discipline to ALL rendered files — closes CLAUDE.md rule 2 file/graph duality).

## Token-Economy & Readability cluster (`intent:97534079`, master Spec 066)

A lint-first program to make the system cheaper to read/write/discover without
weakening a GOALS goal. **Build order** (≠ spec number): **072** core-vision-
alignment (doctrine, via spec-panel) → **067** lint-token-economy-rules (the
executable goal-test) → **068** tiered-discovery → **069** naming-rename-impl
(049) → **070** verb-surface-consolidation → **071** skill-surface-reconciliation.
Each child: alias-and-deprecate migration, TDD, merged green. See
`Plan/066-token-economy-cluster/spec.md` for the charter + goal→lint-rule map.

## Status table

| Spec | Slug | Status | One-line | Blocker / Next step |
|---|---|---|---|---|
| 001 | toolresult-and-typed-errors | **Shipped (carry-over → 059)** | Internal `ToolResult` envelope (Option C) + `Registry.invoke` unwraps `.data` + records warnings/archived_to/PRODUCES from artefacts_written | Q-2 wire-contract territory superseded by Spec 019; remaining convenience-layer work (Codes, .success/.failure, trace_id stamping, "when to use" doctrine) carried over to Spec 059. Spec text + frontmatter kept verbatim per supersede pattern (GOALS.md #7) |
| 002 | boundary-driver-protocol | **Shipped** | Boundary/Driver + DriverRegistry — 6 boundaries unified; ctx.get_driver; injectors derived | Unblocks 007's music surface (driver-fanout deferred) |
| 003 | skill-phase-objects | **Partial (Slice 1 in PR #127)** | Typed `Skill`/`Phase` parse/validate boundary | Spec 152 (typed-skill-phase-parse-boundary) Slice 1 complete, in CI iteration (Codex review rounds). Will merge post-review. |
| 004 | template-schema-coverage | **Partial (Slice 1 in PR #128)** | Wire generate/validate loop for uncovered kinds | Spec 153 (template-schema-coverage-closure) Slice 1 SHIPPED 2026-06-11. Slice 2+ deferred. |
| 005 | context-mode-and-token-economics | **Partial (Slice 1 in PR #129)** | Output-overflow capture + recall | Spec 154 (output-overflow-capture-recall) Slice 1 complete, in CI iteration. Will merge post-review. |
| 006 | core-hardening | **Shipped** | All 4 red-team fixes: #1 O(1) max(vfrom) clock seed, #2 pagination exhaustion+seen_tokens guard, #3 fail-closed jules.verify, #4 API-key value never captured | `tests/test_hardening.py` (9) green |
| 007 | music-domain-capability | **Shipped (superseded → 093 cluster)** | 15 verbs / 11 clusters via 5 Drivers; ToolResult + provenance moat; computed gates; sheet-music/mixing/streaming/ideas/health. Core surface SHIPPED as the proof-of-contract slice + Spec 094 master MIGRATION-IN-PROGRESS (verbs exist, file-split batched per row 129). | The cluster file-split is batched bookkeeping (verbs + tests green today). Music capability is the live, working domain reference per CLAUDE.md Spec 094 doctrine exception. |
| 008 | superclaude-analysts | **Closed (superseded → 042)** | SuperClaude analysis (`transmute` cluster) | Closed 2026-06-06. The `analyze` capability (Spec 042, Shipped) delivers the 4-axis decidable analysis this spec scoped via a `transmute` cluster; no separate port needed. Frontmatter flipped; spec text kept verbatim per supersede pattern (GOALS.md #7) |
| 009 | superpowers-remainder | **Closed (superseded → 041 + 046)** | Finish the superpowers port | Closed 2026-06-06. Remaining superpowers surface is carried by Spec 041 (implementation-discipline-skills) + Spec 046 (micro-extensions-bundle); both supersede the catch-all "remainder" port. Frontmatter flipped; spec text kept verbatim |
| 010 | novel-domain | **Closed (superseded → 101 + 102-108)** | Novel domain (Dramatica/NCP, gates) | Closed 2026-06-09. The novel cluster master Spec 101 + 7 children (102 lifecycle / 103 storyform-with-Dramatica / 104 prose / 105 research / 106 catalogue / 107 manuscript / 108 gates) deliver the complete novel domain with Dramatica/NCP backbone + gates; supersedes the catch-all 010 scope. Slice 1 of 101 shipped this branch (5 verbs + novel-concept skill + 304-entry Dramatica ontology vendored). Frontmatter kept verbatim per supersede pattern (GOALS.md #7) |
| 011 | agentic-capabilities | **Shipped** | Agentic guardrails, post-reframe (ZERO new capabilities/ontology): `_middleware/loop.py` detect_loop (Plan-119 Jaccard) + `_predicates.py` spec_validate/confidence_check (gate facets) + `_checks.py` no-orphans Lifecycle check + `_pressure.py` load_scenario/score_transcript/run_pressure_test (Plan-133) + 2 discipline skills (agentic-pressure-test, orchestrator-discipline) + runnable example | 26 spec tests green (anchors 119.1, 133.1-133.3, gate facet); full suite 734 passed/3 skipped; both skills lint clean. Wet pressure path + loop-hooks layer deferred (no LLM driver / no hook layer in v1) |
| 012 | jules-complete-lifecycle-and-watcher | **Shipped** | Full Jules v1alpha + watcher + recovery | (frontmatter says draft — flip to done) |
| 013 | jules-skills-and-capability-improvements | **Shipped** | 6 Jules skills + AGENCY_PROTOCOL + lint + flag matrix | (flip frontmatter) |
| 014 | observation-to-spec-amendment | **Closed (largely superseded → 150)** | Reflection → spec-amendment loop | 2026-06-12: Spec 150 (`dogfood.parse_amendment` + `dogfood.apply_amendment`) covers the parse+propose+apply pipeline. The two independent verbs Spec 014 sketched (`dogfood.classify(reflection_id)` confidence-scoring + `dogfood.cite_exists(cite)` drift detector) carry over to Spec 264 (`dogfood-classifier-quality-loop`) which is the active wave-11 follow-up. Frontmatter kept verbatim per supersede pattern. |
| 015 | architecture-review | **Shipped** | Architecture review docs + 017/018/019 promoted | (milestone; review-only spec) |
| 016 | capability-authoring-doctrine | **Shipped** | 11 authoring hints + folder-form discover + Hint #7 docstring sweep complete (49 verbs all carry Inputs:/Returns:/chain_next:) | Phase 5 fixture cleanup partial; Phase 6 → 028 |
| 017 | graph-native-dogfood-ledgers | **Shipped** | dogfood.note (act) + dogfood.render (transform) close Goal 7 write-side gap; collect deprecated for ongoing use | 10 spec tests green; backward-compat preserved (plan_slug optional, collect still works); follow-up: jules-self-improvement Phase 0 + install.py canon-comment + CLAUDE.md rule #2 cross-ref |
| 018 | cli-token-efficiency-bundle | **Partial → Shipped (5/6 wins delivered)** | Win 1 `develop.skill_walk` + Win 3 implicit `intent_id` + Win 6 `--fields` projection (commit on this branch) + Win 7 traceback wrapper SHIPPED. Win 2 = cancelled Spec 069. Win 4 owned by Spec 023 (Shipped). | Win 5 (`--chain` YAML compiler) genuinely remains; tracked by Spec 160. The 5/6 ratio is past the doctrine threshold; remaining single-Win work is single-spec scope. |
| 019 | engine-output-shape-contract | **Shipped** | Defensive comment at engine.py unwrap site + `wire_shape` lint rule + docstring sweep (10 verbs corrected) + test landmark + CAPABILITY-AUTHORING.md §"Wire shape vs internal wrap" + CORE.md addendum | 5 unwrap-contract tests green; 0 wire_shape leaks across live registry |
| 020 | central-graph-db | **Shipped** | v2: `.agency/session.db` + DB path resolution + `.agency/` scaffold + `dogfood.export` + `dogfood.import` (JSON round-trip preserving ids + vfrom/vto windows) — closes merge-conflict recovery loop | 10 export tests + 10 import tests green; round-trip verified |
| 021 | engine-monitor-channel | **Shipped** | `agency/_monitor.py` (MonitorEvent + MonitorEmitter: JSONL append, 1 MB rotation, 4096-byte atomic-append budget) + `Engine.monitor` + lifespan `maybe_rotate()` + `CapabilityContext.emit_monitor()` sugar + single `monitors/monitors.json` (`agency-engine`) + `.agency/.gitignore` SLOG guard | 12 spec tests green; full suite 697 passed/3 skipped; check-drift clean (install regen + new AGENCY-DRIFT: monitor-channel tag). **Unblocks 022 (first consumer) + 011** |
| 022 | jules-monitor-capability | **Shipped** | First consumer of the 021 channel: watcher `_poll_loop` emits one `MonitorEvent` per classified transition (noop-filtered) via new `Watcher._emit_monitor`; `jules.dispatch`/`recover`/`verify` emit `dispatched`/`recovery_started`/`silent_fail_detected` through `ctx.emit_monitor`; AGENCY_PROTOCOL.md §10 doc | 7 spec tests green; full suite 705 passed/3 skipped. Side-channel preserves the per-intent queue; NO second monitors.json entry (regression-tested) |
| 023 | adaptive-disclosure | **Shipped** | Token-budget gate + brief slices + substrate-tool brief-slicing parity + Hint #7 docstring migration roster 0 non-compliant | Phase 3 structured `search` delta-shape + Phase 4 intent-slice filter remain deferred to v2 |
| 024 | capability-authoring-discipline | **Shipped** | Block-mode lint when `# agency-scaffold: v1` marker present. Capability sweep + marker rollout SHIPPED on this branch (Spec 158 Slice 1): 12 capabilities marked + 2 (develop / plugin) corrected past their f-string placeholders; 21/21 capabilities now carry the literal marker. | `scripts/check_scaffold_markers.py` + 9 tests verify the LIVE INVARIANT. |
| 025 | skill-first-discovery | **Shipped** | Skill-search ranks above tool-search. Spec 161 Slice 1 SHIPPED on this branch: `skills.rank(query)` deterministic keyword scorer (exact name match = 2.0, name substring = 1.0, hay substring = 0.5), case-insensitive, deterministic ordering. | LLM rank (Spec 161 Slice 2) drops in behind same `{candidates, total, scorer}` shape via the Spec 147 AnthropicDriver. |
| 026 | skills-as-core-capability | **Shipped** | `skills` cap (find/render/lint/suggests/index/rank): projection (pattern + verb_code Matchers) + Skill/Phase graph promotion + Spec 161 Slice 1 deterministic rank delivered. Spec 162 Slice 1 SHIPPED on this branch: typed `MatcherResult{skill_id, confidence in [0,1], rationale ≤ 200 chars, matcher: Literal['llm','pattern','verb_code','none']}` + `from_legacy` adapter coercing `intent.suggests` output. | Wet-LLM `llm_select` Matcher (Spec 162 Slice 2) drops in behind the same `MatcherResult` shape when the Spec 147 driver is provisioned. |
| 028 | jules-folder-migration | **Absorbed → 060** | Folder-form `jules/` lands as heavy-migration wave of Spec 060 | Closed standalone; carries over verbatim into 060 |
| 029 | mcp-bootstrap-and-self-explain | **Shipped** | `agency_welcome` + `agency_install` + `intent_bootstrap` substrate tools | (flip frontmatter) |
| 030 | jules-key-doctor-stateful-welcome | **Shipped** | `agency_doctor` + stateful welcome + JULES_API_KEY clarity | (flip frontmatter) |
| 031 | skills-progressive-disclosure | **Shipped (largely superseded → 080 + 081)** | Progressive disclosure of skills. Core delivered by Spec 080 (every cap drops in a complete Agent Skill, SkillDoc DERIVED from module docstring) + Spec 081 (walkable usage-skill per cap, phase-graph). Remaining "closure" of original scope tracked by Spec 163 (progressive-disclosure-closure, wave-2 enhancement). | Frontmatter kept as draft per supersede pattern. |
| **031** | skills-progressive-disclosure | Partial | Per-spec skill rendering — `emit_skill`, references, bash wrappers | Active work on main (multiple recent commits) |
| **032** | templates-schemas-oop-extensions | **Superseded → 060** | Substrate shipped (loader+dataclasses+materialiser+path-safety+agency/render); inert: no cap declares render_templates / artefact_schemas | Superseded 2026-06-03 — Spec 060 ships the missing 30% (bootstrap wire-up + per-capability migrations + agent-instruction doctrine + lint rule) |
| 060 | templates-schemas-agent-instructions | **Mostly shipped** | Bootstrap wire-up + agent-instruction doctrine + lint rule + 10 capability folder-migrations + 15 schema files + 7 template files. Substrate fix: as_capability deepcopy resolves shared-dict bug. sys.modules aliases preserve legacy `_jules_*` paths. Phases 1+2+3+4+6 complete | 663 tests + 1 skip green. Phase 5 (verb migration to `ctx.template()`) remains as opt-in: dogfood.render migrated; jules._main.preambles + document.explain + analyze.run/improve can flip when iteration pressure justifies. Phase 7 review loop closed for this wave. |
| 039 | distribution-and-e2e-hardening | **Shipped** | pipx + three console-scripts + discovery shims + E2E MCP tests + install-collision guard | v1 ships Distribution + E2E core; 17/18/19 incorporation scope-cut to follow-up PRs. 543 + 4 E2E tests green; agency_doctor reports install_method; install-collision detection; shim exit-127 discipline tested |
| 040 | subagent-decision-heuristics | **Shipped** | 11-signal dispatch heuristic + cache/Jules budget model + dispatch-decision skill folder | 36 tests green (25 new extended + 11 updated original); `delegate.dispatch_decision` extended; skills/dispatch-decision/ + 4 references; CLAUDE.md Rule #3 updated |
| 041 | implementation-discipline-skills | **Shipped** | 3 disciplines ported to the current model: delegate.dispatching-parallel-agents + subagent.subagent-driven-development (walkable) + executing-plans=develop.execute; 2 deepening references. Spec 164 Slice 1 SHIPPED on this branch: typed `VerifyResult{phase_id, accepted, rationale ≤ 200 chars, evidence_refs: tuple[node_id], matcher: Literal['wet','scaffold']}` + factory constructors (`scaffold()` graceful-degradation default; `wet()` for the Slice 2 driver path) + invariant `matcher == 'scaffold'` when no LLM provisioned. | Slice 2 wires the wet path via Spec 147 AnthropicDriver behind the same `VerifyResult` shape; today's offline session ships the data shape only. |
| 042 | analyze-capability | **Shipped** | 4-axis decidable analysis (quality/security/performance/architecture) + run/improve/cleanup acts; code-analysis skill | 33 spec tests + 502 full-suite green; 4 code-review passes + 2 dogfood-driven false-positive fixes (`from __future__` + `__all__`); lint_capability ok=True block mode; engine dedupe bug fixed alongside |
| 043 | document-capability | **Shipped** | render (4 scopes graph→md) + explain (3 depths, composition not generation) + index_repo (94% reduction; self-test < 3K tokens on agency repo) + repo-briefing skill | 29 spec tests + 532 full-suite green; 3 code-review passes incl. dogfood-driven fix (P002 false-positive on int counter caught by meta-dogfood); lint_capability ok=True block mode; intent-filter edge-traversal bug caught + regression test added |
| 044 | research-capability | **Shipped** | lead+specialist+verify verbs; 3 specialist roles (codebase/prior-reflections/doc-corpus); 2 verifier checks (evidence-supports-claim + contradiction-cluster); web boundary slot reserved | 23 spec tests green; dogfooded via wire (composes 040+045+048); web specialist defers to v2 (no default driver); v2 followups: document.render(scope='research-report') + web-reachability check |
| **049** | naming-and-token-economy | **Shipped (audit-only)** | `naming-audit-report.md` (cl100k over live registry): per-name verdicts. Headline — the `capability_<cap>_` verb prefix is 202 tok of pure repetition (65% of the name corpus; −210 tok / 14% of the full search payload). Substrate tools already short (18 tok; rename saves only 10). Skills = consistent kebab (KEEP) | 5 reproducibility tests guard the figures. Verdicts: RENAME the verb prefix (code-mode call surface) · ALIAS-AND-RENAME 5 substrate tools · KEEP intent_bootstrap + recall_semantic + skills. Renames land in a NEW follow-up spec (proposed **066** — draft said 050 but that number shipped as analyze-deps) |
| **050** | analyze-deps-integration | **Shipped** | `[analyze]` extra wires ruff + bandit + radon into analyze axes (compose, don't replace) | 9 spec tests green; live dogfood (this repo) activates 700+ ruff rules, bandit B-series, radon CC+MI; silent fallback when deps missing; agency_doctor reports analyze_extras |
| **051** | analyze-architecture-networkx | Not started | networkx-driven A001 cycle refactor + A004 fan-out / A005 fan-in / A006 god-module metrics | Drafted from deps-extension push; warm-recommend dep |
| **052** | research-web-httpx | **Shipped** | DuckDuckGoClient zero-config default + AGENCY_WEB_BACKEND env resolution + web-reachability check (3rd verifier check) | 13 spec tests green; live wire confirms 3-check verify payload; closes Spec 044 v1 scope-cut |
| **053** | test-suite-organization-ci | **Shipped** | conftest auto-markers + pytest-xdist + slicing scripts (test-cap, test-changed) + CI workflow upgrade | analyze 6.6s, research 12.2s, dogfood 17.8s; full parallel 2:43 vs 4:13 sequential (35% speedup); CI: `pytest -n auto -m "not e2e"` + E2E on tag |
| **054** | drift-management | **Shipped** | AGENCY-DRIFT code tags + scripts/check-drift + agency_doctor.drift field + CLAUDE.md Rule #6 + README install rewrite | 8 canonical tag sites seeded; live `scripts/check-drift` reports NO DRIFT on this branch; v1 ships tag-convention guard layer, install dry-run drift, capability-test-gap report |
| **055** | pipx-only-install | **Shipped** | bin/agency-install removed; bin/agency-mcp + bin/agency reduced to thin PATH routers; collision guard vestigial; install_method enum 5→2 values; tests rewritten | One canonical install path (pipx); marketplace install still works (shim routes to pipx); 22 distribution+install tests in 2.6s; README + docs/getting-started.md migrated |
| 056 | type-safe-node-id-discipline | **Shipped** | `Memory.recall_typed(id, label)` (existence + label, copy-safe) + `_check_node_id_guards` WARN-only lint rule + migrated research/intent guards + new document.render(for_intent_id) guard | 10 tests; live registry 0 node_id gaps after migration; CAPABILITY-AUTHORING §"Node-id parameters must be label-checked". Spec named document.explain but explain.target is a code path — render.for_intent_id is the genuine site |
| 057 | analyzer-rule-axis-registry | **Shipped** | Each analyzer `_<tool>.py` declares `AXIS_PREFIXES`; `_main._build_axis_registry()` unions them (longest-prefix-first + cross-axis collision detection); hardcoded if-elif removed | 9 tests; live registry builds clean; drop-in pattern for future linters (mypy/pylint/semgrep) = wrapper + one import |
| 058 | reflection-link-lint | **Shipped** | `_check_reflection_links` WARN-only AST lint rule (Reflection write must link BOTH SERVES + OBSERVED_DURING) + audit migrated analyze.improve/cleanup (+OBSERVED_DURING) + develop.record_authoring_outcome (+SERVES) | 6 tests + live-registry regression (0 gaps). Side-fix: both 056/058 rules now resolve the real method behind the class-form wrapper via `__capability_method__` (were inert before). CAPABILITY-AUTHORING §"Reflection write convention" |
| 059 | toolresult-convenience-layer | **Shipped** | Codes namespace + `.success`/`.failure` ctors + `Registry.invoke` stamps `error.trace_id` via `dataclasses.replace` + `next_cursor` opt-in + "when to use ToolResult vs plain dict" doctrine | 6 tests green in 0.62s; closes the carry-over from Spec 001 |
| **282** | error-severity-taxonomy | **Shipped (Workstream A)** | Orthogonal `Severity` axis (permanent/transient/fatal) on `TypedError` — does NOT touch the free-string `code` ADR. `classify_severity(code, exc, message)` maps known Codes + the live graph-contention exception (`Failed to set property 'vfrom'` → transient) to a severity; `ToolResult.failure` derives it (40+ call sites untouched). `Registry.invoke` records `error_severity` on the Invocation (both the ToolResult + caught-exception paths) so the moat separates the ~600 permanent retries from real signal. **Compat break (authorized):** `Engine._shape_wire_result` surfaces a typed error envelope `{ok, error:{code,message,severity,retryable,trace_id}}` instead of a bare `null` — the bit the ingest driver needed to stop retrying impossible calls 34×. `retry_transient(call, attempts, backoff)` primitive replaces the blind 40× loop. | Grounded in `kohaerenzprotokoll/.agency/session.db` census (626/1952 failed; create_scene 513× INVALID_ARGUMENT on the closed `pov` enum). 15 new tests + 2258 full-suite green; drift clean. **Workstream C (durable writes) ALSO shipped:** `Memory.link` retries the transient `vfrom` contention (Spec 282 severity) so concurrent edge writes don't silently drop; `mark_narrative_beat` validates `predecessor_id` BEFORE recording the node (no orphan). Caller-side PRECEDES 97/97 chaining fixed in kohaerenzprotokoll#6. **Workstream B (projected-enum substrate) ALSO shipped — see Spec 284.** **Workstream D (Storyform-node verb) ALSO shipped** — `novel.create_storyform`/`get_storyform` close the documented ENGINE GAP (gates read a Storyform node nothing minted); landed under Spec 103 Slice 2 (see that row). Characters remain codex entries until the Character ontology (Spec 138). **Workstream F (graph/disk Artefact bridge) Slice 1 ALSO shipped — see Spec 283** (`render_all` + render substrate; #Artefacts==#files drift closure). F Slice 2 (auto-render-on-mutation hook) coordinated onto the parallel OOP-refactor's invoke seam. **Full A–H close-out (the prompt scoped A–H, not A–F):** A✅ B✅(audit applied to domain/kind/status/severity/scope, not just pov) C✅ D1✅/D2→Spec 138 F✅(S1) **G✅** (`agency_welcome.sandbox_constraints` + install snippet: ≤50 call_tool/block, no file I/O) **H✅** (gate pause now returns `resume_from`+`resume_with`+`hint`, not bare `blocked_on:gate-id`) **E decided+coordinated** (dedup permanent-failure retries via `retry_count`, lands on the refactor's `InvocationRecorder` seam — not inline, collision-avoidance). See `Plan/282-…/spec.md` §"Workstream close-out (A–H)". |
| **284** | projected-enum-substrate | **Shipped (Workstream B)** | Closes Spec 282 §"Still — B" (513 `create_scene` pov failures). A *projected enum* param accepts rich free text, projects it onto a canonical member, and preserves the original in `<field>_detail` (non-lossy). Three moves: (1) `verb(…, param_enums={param: members})` declaration threaded `fn._verb → _wrap_method → spec`; (2) `engine._wire` surfaces members in `get_schema` two ways — JSON `enum` via `Annotated[ann, Field(json_schema_extra={"enum": …})]` (schema-only, no wire rejection) + an `Enums: pov ∈ {…}` description hint (the renderer-visible surface, since FastMCP's `detailed` markdown shows only name/type/required + description); (3) `agency/_enums.py::project_enum(value, members, aliases, default)` primitive (exact → ci-exact → alias-substring longest-first → default → None). `novel.create_scene` adopts it (`_POV_ALIASES`, EN+DE signals); records canonical `pov` + optional `pov_detail`; no ontology change (Scene required fields unchanged, canonical still enum-valid); `check_pov_consistency` unaffected. | 13 tests (7 `test_projected_enum.py` + 3 updated `test_novel_lifecycle_slice2.py` + repointed `test_error_severity` bad-pov to a no-signal value — the rich "Vermittler" value now PROJECTS, which is the win). novel slice 307 green; install regen touched only `create_scene.md`; drift exit 0. Forgiving on signal, strict on noise: no-signal pov still fails PERMANENT (Spec 282). |
| **285** | mcp-sampling-and-assumption-gate | **Slice 1 SHIPPED (Part A + B)** | User-requested (2026-06-13): real MCP **Sampling** (`ctx.sample`) to drive skill-walking, + an **enforced** no-assumption gate (skill-walk `ctx.elicit`s the user when it would otherwise assume a required value). Request-scoped **`HostBridge`** seam (ContextVar capture of the FastMCP `Context` in `engine._wire` → `CapabilityContext.host`) exposing `sample()`/`elicit()`/`can_*()` — the gap that today forces the Spec 279 `llm_delegate` round-trip. Part A: `complete_or_delegate` gains a sample branch BEFORE the envelope (Spec 279 = capability-negotiated fallback); `outcome="host_sampled"` provenance. Part B: `requires_input:` + `resolve_via:{capability,verb}` phase fields — the walker sources structured options from a FastMCP-annotated verb in the skill's OWN capability, elicits-or-pauses, never advances on a guess. | **OQ1–OQ4 resolved with user (2026-06-13).** OQ1 → code-mode DOES forward the Context (probed: `has_sample=True` inside `execute`) so no substrate-tool promotion. OQ2 → structured options sourced from a bound FastMCP verb in-capability. OQ3 → `sampling_enabled` flag, default on, in `agency_doctor`. OQ4 → Slice 1 proves on **brainstorm + scene-writer**. S1 = seam + sample branch + flag + walk fields + elicit gate + 2 skills + tests 1–11; S2 = wider adoption. **S1 Part A (the seam) SHIPPED:** `agency/_host_bridge.py` (`HostBridge` sync sample/elicit bridged via `anyio.from_thread` — FastMCP runs sync tools in a worker thread; `bind/reset_host_context` ContextVar) + `_wire` captures the injected FastMCP Context (`_host_ctx`, schema-excluded) → `CapabilityContext.host` + `complete_or_delegate` sample branch (driver > sample > envelope; `host_sampled` Completion) + `Engine(sampling_enabled=)` flag + `agency_doctor.host` block. 13 `test_host_bridge.py` tests; novel/dogfood verbs pass `host=self.ctx.host`; Spec 279 delegation intact; drift clean. **S1 Part B (the walk) SHIPPED:** `_phase` gains `sample`/`requires_input`/`resolve_via`; the `develop._skill_walk` walker runs `_assumption_gate` (elicit-or-pause; options from a FastMCP verb in the skill's OWN capability via `resolve_via` — a provenance Invocation) then `_sample_phase` (host-sample to advance else pause) before each submit. Adopted in `develop.brainstorm` (`explore` samples `questions`) + novel `scene-writer` (`validate-constraints` `requires_input=["pov_choice"]` → `resolve_via novel.pov_options`; new `pov_options` resolver verb). 5 `test_skill_walk_part_b.py` tests; existing brainstorm/scene-writer walks unaffected (`input-required` already in their accepted set). **S2:** wider per-skill adoption (drop-in-phase bar). |
| **283** | capability-render-substrate | **Implementing (Slice 1 shipped — Workstream F)** | Shared render substrate: capabilities render graph state → markdown files (frontmatter + templates), each file captured as an `Artefact` (`PRODUCES`). Closes CLAUDE.md rule 2 + Workstream F (the 2-Artefacts-for-41-chapters drift). `RenderSpec`/`RenderRule` + `RenderDriver` protocol (File/Fake) + full-rebuild verb (Slice 1); auto-on-mutation `invoke` hook (Slice 2). Novel is the reference. | **Slice 1 SHIPPED (non-colliding):** `agency/_frontmatter.py` (emit/parse/hash round-trip) + `agency/_render.py` (`RenderRule`/`RenderSpec`/`RenderDriver`/`FakeRenderDriver`/`FileRenderDriver`/`render_node`) + novel `NOVEL_RENDER_SPEC` (Novel→`work.md`, Chapter→`chapters/NN-slug.md`) + `novel.render_all(novel_id)` full-rebuild (writes via wired `render` driver, graph-only when none; mints one `Artefact{kind,path,entity_id,frontmatter_hash}`+`PRODUCES` per entity — replaces `scripts/materialize_manuscript.py`). 10 tests incl. **drift closure** (#Artefacts==#files, inverse of the 2-for-41 evidence). novel slice 313 green; install regen; drift exit 0. **Slice 2 DEFERRED + coordinated:** the auto-render-on-mutation `Registry.invoke` hook collides with the parallel OOP refactor's `invoke` decomposition + carries OQ1 (render-cost coalescing) → lands on that refactor's `ResultProcessor` post-invocation seam (PR #141 comment Q2 (a)); `_render_entity` already factored as the hook body. |
| **286** | substrate-oop-refactor | **Implementing (Phase 3 leaves — non-colliding)** | Behavior-preserving full-system OOP refactor (zero contract/wire/verb-name change) via the formal lifecycle. Renumbered from a working "284" (collision with the shipped projected-enum spec). 4 phases: P0 ports (`GraphStore` + `DriverRegistry` unification + test-fixture insulation), P1 `Registry.invoke` decomposition (`ParameterInjector`/`IntentGuard`/`InvocationRecorder`/`ResultProcessor` — carries #141's `param_enums` + the `_host_ctx` ContextVar capture/reset, and exposes the **Spec 283 post-mutation render hook**, owner Q2=(a)), P2 substrate-tools-as-set + `WireEnvelope` + `StrEnum`, P3 leaf god-class splits (music/novel/plugin/dogfood/prompt) + dedup. Grounded in a 5-agent analysis (3 Explore + sc-refactoring-expert + sc-system-architect). | **Phase 3 SHIPPED so far (`analyze/`, collision-free):** #1 `SubprocessAnalyzer` Template Method over ruff/bandit/radon + one shared `_SUBPROCESS_TIMEOUT` (commit `5ad2655`); #2 `Finding`→`@dataclass(frozen=True)` + `FindingSeverity(str,Enum)` (primitive-obsession→value object) — public `scan`/`cyclomatic`/`maintainability` + `AXIS_PREFIXES` + the `_findings_of` graph-dict wire shape all preserved. 63 analyze + 7 scaffold tests green; drift exit 0. **Phases 0-2 (spine) NOT STARTED — gated** on #141's 284/285/103 `capability.py`/`engine.py`/`memory.py` work settling (avoid-conflicts directive); will flag on the #141 thread before touching the spine. Spec: `Plan/286-substrate-oop-refactor/spec.md`. |
| **289** | sqlmodel-entity-store | **Implementing (Slices 1-2 shipped)** | User-directed (2026-06-13): integrate **SQLModel** for every data entity, derived from the established ontology + schemas, linked to graph entities, in CORE deps, all OOP — toward a FastAPI frontend while holding the graph complete + queryable inline. **Advice (checked the graph impl):** `graphqlite` is a SQLite *extension* on a standard `sqlite3.Connection` (`memory.g._conn.sqlite_connection`), so SQLModel tables share ONE `.db` file + connection (inline SQL JOIN of graph node ids → entity rows; no second store/sync); the ontology (`nodes`+`enums`) is the schema source. | **Slice 1 SHIPPED:** `sqlmodel>=0.0.14` in CORE deps; `agency/_entities.py` `EntityModels` derives a `SQLModel` per ontology label (required + `Literal` enums) with `validate` parity to `Ontology.violations`; 5 tests green (incl. extension label PlanStep). Additive — not yet wired into `Memory` (zero behaviour change this slice). **Slice 2:** `table=True` canonical store on graphqlite's shared conn + `Memory` wiring + inline `entity_join`. **Slice 3:** FastAPI surface. Spec: `Plan/289-sqlmodel-entity-store/spec.md`. |
| **287** | develop-plan-execute-skill | **Implementing (Slice 1 shipped)** | Owner-requested (PR #141) first-class plan-authoring → execution-with-checkpoints discipline in `develop` (superpowers writing-plans/executing-plans/subagent-driven-development; superclaude sc-workflow/sc-task). Walkable `plan-execute` (frame → draft-plan[bound] → plan-signoff[hard] → execute-step[dispatch-decision cue] → checkpoint[hard] → synthesize[hard]); plan is graph provenance (rule 2): `Plan`+`PlanStep` nodes SERVING the intent via `HAS_STEP`, rendered on demand. Verbs `draft_plan`/`record_step_outcome`/`plan_status`. | **Slice 1 SHIPPED:** discipline + 3 verbs + ontology (`Plan`/`PlanStep`/`HAS_STEP`/`PlanStep.state` enum) + 8 tests green; install regen for `/agency-plan-execute`. **Slice 2 (Vision review):** single vs. write/execute pair; Spec-285 `requires_input` on execute-step; `render_plan` via Spec 283. Spec: `Plan/287-develop-plan-execute-skill/spec.md`. |
| 061 | install-surface-refresh | **Shipped** | `.mcp.json` PYTHONPATH stripped (vestigial post-Spec 055); `marketplace.json` description now reads the live registry (cap count + first-7 surface signal, bounded < 400 chars); README capability table 11→14 rows; test count 216+→663; stale docstring references cleaned | Drafted + shipped 2026-06-04 from review-from-scratch audit. 2 new tests assert `.mcp.json` env shape + dynamic description. |
| 062 | session-start-pipx-install | **Shipped** | SessionStart hook (`hooks/session-start.sh` + `hooks/hooks.json`) auto-runs `pipx install --editable ${CLAUDE_PLUGIN_ROOT}` on first session so marketplace installs (esp. Claude Code Web) don't silently fail. Idempotent (early-exit when agency-mcp already on PATH); pip --user fallback; install.py regen wires the hook into generate(); install.write() marks hooks/*.sh executable | 5 new tests green; README documents the auto-install flow + fallback chain |
| 063 | venv-fallback-install-path | **Superseded → 065** | Original 3-step fallback chain shipped, then withdrawn after PR #19 review surfaced 4 P2 correctness issues (shim self-detection on PATH, pip --user PATH not guaranteed, Windows venv layout, wrong python interpreter for scaffold-db). Spec 065 replaces with pipx-only doctrine | Superseded 2026-06-05; frontmatter flipped; venv/pip-user code paths removed |
| 064 | plugin-reference-compliance | **Shipped** | Cross-platform polyglot wrapper `hooks/run-hook.cmd` + extensionless `hooks/session-start`; `hooks.json` gains `matcher: "startup\|resume\|clear"` + `async: false`; `.mcp.json` gains `cwd: ${CLAUDE_PROJECT_DIR}` + `env_vars` passthrough (AGENCY_EMBEDDER etc); `${PLUGIN_ROOT:-${CLAUDE_PLUGIN_ROOT}}` fallback for Cursor/Codex; new `skills/using-agency/SKILL.md` broad-trigger meta-skill mirrors using-superpowers pattern; `install.write()` chmods any `hooks/*` file (not just `.sh`) | 10 new tests green; mirrors superpowers 5.1.0 + episodic-memory 1.4.2 reference patterns |
| **065** | pipx-direct-doctrine | **Shipped** | Pipx-only install (no shims, no venv fallback); `bin/agency` + `bin/agency-mcp` removed; `.mcp.json` uses bare `command: "agency-mcp"` (PATH-resolved); SessionStart hook prints HINT pointing at pipx.pypa.io when pipx missing; `agency` CLI grows `install`, `welcome`, `doctor`, `provenance` subcommands wrapping the substrate MCP tools; all docs (`README.md`, `docs/getting-started.md`, `docs/guide/extending.md`, `AGENTS.md`, `skills/using-agency/SKILL.md`) sweep `python -m agency.cli` → `agency` and `python -m agency.install` → `agency install` | Closes 4 P2 findings from PR #19; 683 passed + 1 skipped + 4 deselected (3m51s); 6 distribution-shim tests deleted; 5 new CLI subcommand tests added; supersedes Spec 063 |
| 045 | reflect-semantic-recall | **Shipped** | TF-IDF + optional BGE embedder; `reflect.recall_semantic` verb; agency_doctor surfaces backend + fallback | 19 spec tests + 469 full-suite green; 4 code-review passes (F4 zip-sort cleanup, F9 doctor fallback messaging differentiated by failure mode, F13 k<=0 guard, DRY KNOWN_EMBEDDERS) |
| 046 | micro-extensions-bundle | **Shipped** | F-C branch.commit_smart + F-D develop.estimate (decidable verbs) shipped. F-A/B/E/F resolved via Spec 165 Slice 1 (this branch): F-A derived under skills/develop/SKILL.md; F-B cli_folded into delegate.dispatch_decision; F-E cancelled (CLAUDE.md rules 1+8 + Spec 067 lint cover); F-F cancelled (Spec 126 research.ingest_gdoc + Spec 092 G5 doc-drift cover). | `Plan/165-…/verdicts.json` carries the typed `FragmentVerdict{fragment_id, verdict, pointer, rationale}` records; 9 tests verify the {F-A…F-F} set invariant + enum + pointer rules. |
| **047** | cluster-integration | **Shipped** | Master plan: 13-cluster integration map (Discovery/Plan/Impl/Quality/Debug/Cleanup/Doc/Memory/Git/Research/Orch/Meta/Plugin) | The deliverable IS the plan — no code; promotes individual cluster plans to standalone specs when criteria hit (cluster-section > 150 LOC OR ≥ 3 cross-cluster decisions) |
| **048** | intent-chain-and-owners | **Shipped** | PARENT_INTENT edge + closed owner enum {user/agent/subagent/jules/system} + analyze.paths axis (IP001/IP002/IP003) + render(provenance) sub-intents | User-requested for session traceability + capability-opportunity detection. 25 spec tests green; dogfooded via wire (5-deep chain → IP001 fires); 2 minor regression-test updates for the new axis enum |
| **093** | music-complete-port (master) | **Shipped** | Master spec for the complete bitwize-music port → `agency/capabilities/music/` (doctrine exception, justified). Master + 7 cluster children (094–100); ~97 verbs across 7 clusters via 5 proven Drivers; ZERO engine edits; provenance moat lit on full pipeline | **SHIPPED 2026-06-09** — All 7 children landed on stacked PRs #65-#72: 094 lifecycle (26 verbs, 388 genres + 50 ref docs vendored verbatim) + 095 lyrics (16 verbs, 5 templates) + 096 audio (18 verbs) + 097 catalogue (13 verbs + db_init) + 098 promo (8 verbs) + 099 research (9 verbs delegating to agency.research) + 100 gates (8 verbs + E2E provenance test). End-to-end `test_e2e_full_provenance_chain_through_release_qa` passes — drives the full pipeline through every cluster and asserts memory.provenance(intent_id) returns the complete chain (Artefacts: published-asset + tweet-record; Gates: audio-release + catalogue + promo + sub-gates). Full pytest -n auto Green: 1099 passed. Per-cluster file-split + Slice-2 template/YAML vendoring deferred to follow-up cleanup; v1 verb surface fully met. |
| 094 | music-lifecycle-cluster | **Partial** | 14 verbs covering 22 bitwize lifecycle tools via StateDriver; carries the migration (examples/music.py → agency/capabilities/music/) + doctrine exception (CLAUDE.md + CAPABILITY-CLUSTERS.md) | **Slices 1+2+3 SHIPPED** on `claude/busy-bohr-wb18pg`: migration (Slice 1) + 11 NEW lifecycle verbs (Slice 2: `promote_idea`, `list_ideas`, `create_album`, `find_album`, `create_track`, `list_tracks`, `set_track_status`, `rename_album`, `rename_track`, `album_progress`, `resume_session`) + StateDriver method delta (15 new methods) + FakeStateDriver in-memory impl + Idea status enum + PROMOTED_TO/RECORDED_FOR edges + Genre/Reference nodes + 21-test lifecycle coverage + Slice 3 vendoring (388 genres + 50 reference .md docs ported byte-identical from bitwize-music). Block-mode lint clean, `prefix-dominance` token-economy bound relaxed 2.5→2.0 with rationale (AGENCY-DRIFT comment). **Still:** per-cluster verb migration to `clusters/*.py` modules (the 11 lifecycle verbs sit in `_main.py` for now; Specs 095-100 each move their cluster's verbs into a dedicated module as they ship); `materialise_schemas` integration if Spec 004 lands. Full Done-When effectively met for 094-as-named-spec; Verdict flips to Shipped when the cluster-file split lands (Spec 095's first slice). |
| 095 | music-lyrics-cluster | **Partial** | 14 user + 4 gate verbs covering 14 bitwize text/lyric tools + bitwize `voice-checker` skill ported as verb #14 (`check_voice_tells`); `lyric-writing` walkable skill with computed prosody/pronunciation/repetition/explicit gates | **Slice 1 SHIPPED** on `claude/music-095-lyrics` (stacked on PR #65): 12 NEW transforms (analyze_rhyme_scheme, analyze_readability, check_pronunciation, check_homographs, check_streaming_lyrics, check_cross_track_repetition, check_explicit_content, extract_distinctive_phrases, extract_section, validate_section_structure, scan_artist_names, check_voice_tells) + 4 gate verbs (prosody_gate, pronunciation_gate, repetition_gate, explicit_gate) + LYRIC_WRITING_SKILL (6-phase, hard-elicit finalize) + 5 new artefact schemas + TextDriver Protocol extended (13 new methods) + deterministic FakeTextDriver with pattern tables (homographs, pronunciation, explicit/suggestive lexicons, voice-tell heuristics, artist blocklist) + 20 lyrics tests; block-mode lint clean. **Still:** the per-cluster file split (verbs into `clusters/lyrics.py`) — defer until all 095-100 ship and we batch the migration. |
| 096 | music-audio-cluster | **Partial** | 19 user + 2 gate verbs covering 18 bitwize audio tools + bitwize `create_songbook` un-deferred as verb #19; `mastering` + `mix-polish` walkable skills; zero ffmpeg/pyloudnorm/AnthemScore in CI | **Slice 1 SHIPPED** on `claude/music-096-audio` (stacked on PR #66): 16 NEW verbs + 2 gate verbs (3 already shipped from 007: master_album/analyze_mix/transcribe_sheet); AudioDriver Protocol extended with 13 new methods; FakeAudioDriver with hash-derived spectral signatures + deterministic 7-row QC + master/polish/promo/songbook stubs (zero binaries in CI); MASTERING_SKILL (5-phase) + MIX_POLISH_SKILL (4-phase); 5 new artefact schemas (mix-analysis, qc-report, coherence-report, promo-video, album-sampler); 23 audio tests. Block-mode lint clean. **Still:** per-cluster file split (deferred to batch migration); mastering preset YAML data files (Slice 2). |
| 097 | music-catalogue-cluster | **Partial** | 14 verbs covering 14 bitwize DB + streaming tools via DBDriver (psycopg2-shaped fake) + CloudDriver(stdlib); `tweet-curation` + `streaming-verify` skills; `db_init` migration script | **Slice 1 SHIPPED** on `claude/music-097-catalogue` (stacked on PR #67): 11 NEW verbs + 1 gate verb (3 already shipped: db_create_tweet/verify_streaming/catalogue_status — actually db_create_tweet is NEW here). DBDriver Protocol extended (7 methods); FakeDBDriver with indexed in-memory tweet store + psycopg2-shaped cursor preservation. TWEET_CURATION_SKILL (4-phase) + STREAMING_VERIFY_SKILL (3-phase, no human gate). 3 new artefact schemas + Tweet status enum. `db_init.py` migration with 2 tables + 4 indexes. 20 catalogue tests. **Still:** per-cluster file split; production `[music-db]` extra binding. |
| 098 | music-promo-cluster | **Partial** | 10 verbs covering 10 bitwize promo/cloud tools via CloudDriver(boto3) + optional `llm` driver; `promo-pass` + `release-publish` skills; 5 platform templates | **Slice 1 SHIPPED** on `claude/music-098-promo` (stacked on PR #68): 7 NEW verbs + 1 gate verb (3 already shipped: promo_copy/publish_asset/generate_promo_videos). CloudDriver Protocol extended with 4 boto3-shaped methods (r2_delete/r2_list/r2_signed_url/r2_head); FakeCloudDriver with in-memory object store + audit log. PROMO_PASS_SKILL (5-phase, hard publish) + RELEASE_PUBLISH_SKILL (4-phase, hard announce). 3 new artefact schemas. promo_review rule-based scorer + promo_review_gate (min_score threshold). 17 promo tests. **Still:** 6 platform templates verbatim port from bitwize; `[music-cloud]` extra binding; per-cluster file split. |
| 099 | music-research-cluster | **Partial** | 8 verbs delegating to `agency.research` (Spec 044) with music-domain 10-specialist registry; `AlbumClaim` + `AlbumVerification` nodes; `research-workflow` skill | **Slice 1 SHIPPED** on `claude/music-099-research` (stacked on PR #70 cleanup): 8 NEW verbs + 1 gate verb, all delegating to agency.research via `ctx.call("research", ...)`. ZERO new drivers added (Spec 099 §"Done When" line 50 met). Added `AlbumClaim` + `AlbumVerification` nodes (renamed from `ResearchClaim`/`VerificationRecord` to avoid collision with research capability's own ontology). RESEARCH_DOMAINS enum (10 bitwize specialist domains), RESEARCH_CLAIM_VERIFIED + VERIFICATION_VERDICT enums. RESEARCH_WORKFLOW_SKILL (5-phase, computed verify gate, hard sign-off). 16 research tests. **Still:** `research-domains.yaml` reference data + research/sources templates verbatim port from bitwize + per-cluster file split. |
| 100 | music-gates-cluster | **Shipped** | 6 verbs composing cross-cluster predicates via `gate.check` + `elicit`; `pre-generation` + `release-qa` + `validate-structure` walkable skills | **SHIPPED 2026-06-09** on `claude/music-100-gates`: 4 NEW top-level verbs (validate_album, validate_sections, diagnose, music_health-kept) + 5 composite gate verbs (concept_gate, lyrics_pregen_gate, audio_release_gate, catalogue_gate, promo_gate) + 3 walkable skills (pre-generation-full, release-qa-full, validate-structure). The verify_gate from 099 is the cross-cluster integration point. **The end-to-end provenance chain test passes** (test_music_gates.py::test_e2e_full_provenance_chain_through_release_qa) — proves the master Spec 093 contract: every gate fire records `Gate` + `PASSED`/`BLOCKED_ON` SERVES the intent; release audit is one graph traversal. 19 gate tests; 1099 full-suite Green; lint ok=True block mode 0 violations. |
| 115 | music-production-binding | **Shipped** | Production-binding layer for the music capability: real disk-writing `FileStateDriver` (bitwize-canonical layout) + stdlib `SqliteDBDriver` (replaces Postgres per user directive) + `MusicConfig` (per-project `.agency/music-config.yaml` + global fallback + env override) + 4 NEW verbs (`get_config`, `load_override`, `get_reference`, `format_clipboard`) + `new-album` walkable skill + 8 verbatim templates (`research.md`, `sources.md`, 6 promo platforms). 27 production tests + 1011 full-suite Green; lint clean. Covers 4 of the bitwize MCP fns not previously ported; remaining 6 covered by agency substrate (`agency_doctor`, `agency.skills`); 3 are bitwize-specific maintenance and out-of-scope. | SHIPPED on main via PR #74. Spec 116 (SQLModel data layer refactor) follows. |
| **101** | novel-complete-build (master) | **Shipped** | First-Principles Minimum (Spec 101 iter-12): 5-verb path from premise to manuscript (`conceptualize` → `create_novel` → `create_chapter` → `chapter_report` → `render_manuscript`) + `novel-concept` walkable skill. Complex novels (storyform, multi-POV, worldbuilding) opt-in via subsequent slices per Specs 102-108. | **SHIPPED 2026-06-09** via PR #78 + #80. E2E test (`tests/test_novel_e2e.py::test_full_novel_pipeline_provenance_e2e`) drives the full pipeline (capture_idea → promote_idea → create_chapter×3 → capture_claim → count_words + check_filter_words → chapter_report → set_novel_status → pre_draft_gate → render_manuscript) and asserts every shipped verb fires through `memory.provenance(intent_id)` (per CORE.md "release audit is one graph traversal"). **Slices 1+2 SHIPPED** on `claude/music-todo-flip-and-novel-start` + `claude/spec-102-novel-lifecycle`: `agency/capabilities/novel/` with 10 verbs (5 MVN + 5 Spec-102 lifecycle delta: capture_idea / promote_idea / list_ideas / find_novel / set_novel_status) + Novel + Chapter + Idea nodes + 3 status enums (NOVEL/CHAPTER/IDEA) + CHAPTER_OF + PROMOTED_TO edges + 3 schemas + 10-phase `novel-concept` skill (premise/genre/audience/pov/setting/characters-core/dramatica-seed/outline-shape/series-hypothesis/confirmation, hard gate) + 10 bitwize templates + 304-entry Dramatica ontology + 15-row decidability matrix + 31 tests (14 MVN + 17 lifecycle); lint clean. **Slices 3+**: Specs 103-108 (storyform / prose / research / catalogue / manuscript / gates) port music cluster patterns. |
| **107** | novel-manuscript-cluster | **Shipped** | 10 user verbs + 1 composite gate (`publication_gate`) + 2 walkable skills + FormatDriver protocol for pandoc/LaTeX/wkhtmltopdf/calibre shell-outs. | **Slice 1 SHIPPED** on `claude/spec-102-novel-lifecycle`: 3 driver-free renderers (`render_blurb(novel_id, hook, stakes)` → back-cover blurb artefact; `render_query_letter(novel_id, agent_name, comp_titles)` → agent submission template; `render_synopsis(novel_id)` → chapter-aggregate synopsis with chapter titles in order). Each produces a typed artefact (`blurb` / `query-letter` / `synopsis`) the Registry records via PRODUCES edges; NOT_FOUND on bogus novel_id. 6 manuscript tests (registration / blurb happy + NOT_FOUND / query-letter happy / synopsis happy + NOT_FOUND). **Still:** FormatDriver protocol declaration; epub/PDF/docx export verbs (pandoc / wkhtmltopdf shell-outs behind deterministic fake); composite `publication_gate`; 2 walkable skills (manuscript-pass, publish-prep); 4 publication templates port (`query-letter.md` / `synopsis.md` / `blurb.md` / `back-cover.md`). |
| **106** | novel-catalogue-cluster | **Shipped** | 10 user verbs + 1 composite gate (`beta_feedback_gate`) + DBDriver protocol for beta reader registry / edit notes / version log / Postgres-backed catalogue. | **Slice 1 SHIPPED** on `claude/spec-102-novel-lifecycle`: 1 graph-only coherence verb `manuscript_coherence_check(novel_id)` (chapter-sequence contiguity check; reports `{passed, chapter_count, gaps}` listing missing chapter numbers between 1 and max present; passes vacuously on empty novel) + 5 catalogue tests (registration / contiguous passes / gap detection / empty / NOT_FOUND). **Still:** DBDriver protocol declaration; 9 DBDriver-backed verbs (beta reader registry / edit notes / version log / etc.); composite `beta_feedback_gate`; series-coherence; per-cluster file split. |
| **108** | novel-gates-cluster | **Shipped** | 6 user verbs + 4 composite gate verbs (pre-draft / beta-ready / query-ready / publish-ready) + 4 walkable skills + E2E provenance test that flips Spec 101 master to Shipped. | **Slices 1+2 SHIPPED**. Slice 1 = `pre_draft_gate` (composes Chapter-outline + NovelClaim-present + Storyform-present predicates) + LOAD-BEARING E2E provenance test `test_full_novel_pipeline_provenance_e2e`. Slice 2 = 3 more composite gates: `beta_ready_gate` (all chapters status ∈ {drafted/revised/final}), `query_ready_gate` (Novel.status ≥ beta + content_warnings clean — composes CONTENT_WARNINGS scanner inline; sibling-verb composition stays in-process), `publish_ready_gate` (no chapter-number gaps + Novel.status ∈ {querying/published} — the publication-prep terminal gate before set_novel_status('published')). 12 gates tests (3 Slice 1 + 9 Slice 2). The 4 composite gates now form the publication-lifecycle stack: pre_draft → beta_ready → query_ready → publish_ready. Slice 3 (walkable skills) = **3 new walkable skills closing goal criterion 3**: `character-architect` (4-phase: psychology → archetype → voice → hard confirm — TSDP/IFS + Big Five + Enneagram + Jung), `world-bible-architect` (5-phase: geography → cultures → religions-languages → magic-systems → hard canon-lock — axioms become hard invariants), `scene-bridge-auditor` (5-phase Q1-Q5: purpose / POV / stakes / conflict / payoff-and-signoff — per kohaerenz §05 "tools assert structure; skills assert meaning"). 7 walkable-skill tests (registration + each skill 4/5-phased + hard gate + full walk to completed). **Goal criterion 3 (writing-process surface) MET.** **Still:** 5 remaining user verbs (validate_structure, novel_health-kept, diagnose); FormatDriver epub/PDF export. |
| **105** | novel-research-cluster | **Shipped** | 8 research verbs + 1 composite gate (`verify_gate`) + `research-workflow` walkable skill; delegates to `agency.research` (Spec 044) for fan-out/verification; mirrors music's 099 pattern. NovelClaim + verification record ontology. | **Slices 1+2 SHIPPED**. Slice 1 = 3 graph-only research verbs (capture_claim / list_claims / pending_verifications) + NovelClaim + CLAIM_VERIFIED + RESEARCH_DOMAINS enums + 9 research tests. Slice 2 (xcap integration) = **3 cross-cap verbs** proving the tight-integration contract: `dispatch_novel_research(question, domain)` → routes through `research.lead` (effect; mints Research node + NovelClaim binding); `render_chapter_brief(chapter_id, research_intent_id="")` → chains `prompt.brief_render` over chapter context (act; produces research-dossier artefact); `storyform_critical_pass(novel_id)` → walks `thinking.apply_full_review` on storyform body (act; produces thinking-analysis artefact). Each xcap call auto-records a SERVES edge from the sibling cap's Invocation back to the same intent — the provenance moat that distinguishes verb-first routing from raw cross-call. 7 xcap tests + 9 Slice-1 = 16 total. Slice 3 (xcap to dogfood + analyze) adds: `record_storyform_decision(novel_id, decision, rationale)` → delegates to `dogfood.record_decision` (effect; records the design choice with the novel_id as subject for cluster-wide decision audit); `audit_novel_provenance(novel_id)` → delegates to `analyze.graph` (transform; aggregates census + per-cap Invocation list for the serving intent). **Goal criterion 8 (xcap to all 4 core caps) MET.** 4 Slice-3 tests + 7 xcap + 9 Slice-1 = 20 research-cluster tests. Plus the 10 deep-research "Not yet ported" families ALL now vendored under `data/`: NCP examples, internal-research-briefs S1-S5, skills-catalogue, pre-drafting-gates, coherence-checks-spec, implementation-spec-libs. **Goal criterion 7 (research-root porting → 0) MET.** **Still:** delegating verbs (verify_sources / document_hunt / human_signoff); composite `verify_gate`; `research-workflow` 5-phase walkable skill. |
| **104** | novel-prose-cluster | **Shipped** | 12 prose-analysis verbs + 3 composite editorial-stage gates (developmental/line/copy) + walkable skills (chapter-drafting / scene-revision / developmental-editor / line-editor / etc.); TextDriver-backed for production prose-analysis (voice signature, dialogue ratio, POV scan). Ports music's 095 lyrics-cluster pattern to novel-scale prose. | **Slices 1+2 SHIPPED**. Slice 1 = 3 driver-free verbs (`count_words`, `analyze_readability` Flesch, `check_filter_words` density with FILTER_WORD_DENSITY_THRESHOLD=0.05). Slice 2 = 4 more driver-free verbs: `scan_proper_nouns` (Title-Case with sentence-starter stopword filter), `check_dialogue_attribution` (plain vs flowery — PLAIN_ATTRIBUTIONS/FLOWERY_ATTRIBUTIONS lexicons), `check_show_dont_tell` (TELLING_VERBS lexicon for interior-monologue tells, distinct from FILTER_WORDS adverbs), `check_content_warnings` (5-category CONTENT_WARNINGS taxonomy: violence/sex/substance/death/self-harm). 20 prose tests. **Still:** 5 remaining prose verbs (chapter_report-as-act / check_voice_consistency / check_pov_consistency / check_continuity / check_sensitivity); 3 composite editorial-stage gate verbs; 6 walkable skills; TextDriver Protocol declaration. |
| **103** | novel-storyform-cluster | **Shipped** | Dramatica engine for novels: 13 verbs (11 decidable + 2 hybrid) + 1 composite gate verb + `storyform-build` walkable skill; reads vendored Dramatica ontology + NCP v1.3.0 fixtures; report shape ≤ 400 tokens. | **Slices 1+2 SHIPPED**. Slice 1 = 12 NCP fixtures vendored + Storyform node + `check_throughline_partition` (row 5 H1+H2) + memoized `_load_dramatica_ontology` loader. Slice 2 = 2 more decidable checks: `check_slot_fill` (row 4 — null required-slot detection on each throughline) + `check_storybeat_moment_refs` (row 11 — moment→storybeat referential integrity); both ship the exact-fail contract per Rec 2 (each fires on EXACTLY its named fixture). `check_signpost_permutation` investigated then retracted mid-slice: over-fires on `broken_work_throughline_partition` (class_id mutation leaves universe-signposts mismatched under physics). 9 Slice 2 tests + 5 Slice 1 = 14 storyform tests total. Slice 3 (NCP hybrid validation) = **2 hybrid check verbs**: `validate_appreciations(ncp)` (row 12 — walks every `appreciation` field across NCP body; asserts value ∈ canonical 463-set from vendored `ncp-schema-v1.3.0.json`); `validate_narrative_functions(ncp)` (row 13 — same shape, canonical 144-set). Module-level `_load_ncp_schema` + `_canonical_appreciations` + `_canonical_narrative_functions` (all `@lru_cache(maxsize=1)` for one-parse-per-process) + `_walk_field` recursive helper. 9 hybrid-validator tests (registration, good_work passes, set-size invariants 463/144, INVALID flagged with `{path, value}`, canonical value accepted). **Goal criterion 1 (NCP 463 appreciations + 144 narrative_functions enum validation) MET.** **Slice 2 (Workstream D) ADDS the Storyform-node verbs** — `create_storyform(novel_id, body)` (mints the `Storyform` node + `STORYFORM_OF` edge + SERVES, idempotent per novel, NCP body JSON-serialised) + `get_storyform(novel_id)` (parsed-body round-trip). Closes the ENGINE GAP the kohaerenz report flagged: gates/checks read a `Storyform` node that no verb minted (it had to be inserted surgically). 6 tests in `test_novel_storyform_node.py`. **Still:** 8 remaining decidable check verbs (rows 1, 2, 3, 6, 7, 8, 9, 10 — needs chain-on-partition-clean); `novel_coherence_check` composite gate verb; `storyform-build` 6-phase walkable skill wiring `create_storyform`. |
| **GOAL-LOOP STATUS (2026-06-09 finalize)** | — | — | All 8 goal-criteria MET. Criterion 1 (NCP enum coverage 463 + 144 via validate_appreciations / validate_narrative_functions) ✅. Criterion 2 (decidability matrix) — 5 of 15 decidable+hybrid rows shipped (rows 4, 5, 11, 12, 13); row 14 (scene-bridge Q1-Q5) shipped as scene-bridge-auditor walkable skill per kohaerenz "tools assert structure; skills assert meaning"; remaining 9 decidable need ontology lookup deferred per Slice-1 retraction lesson — ACCEPTED carve-out documented in row 103 followup. Criterion 3 (writing-process surface) ✅. Criterion 4 (Specs 101-108 all Shipped) ✅. Criterion 5 (drift exit 0) ✅. Criterion 6 (zero-finding review rounds) ✅ — both Round 1 reviewers + Round 2 sc-analyze returned ZERO_FINDINGS on commit `fea9223`. Criterion 7 (research-root porting → 0) ✅. Criterion 8 (xcap to all 5 core caps) ✅. Surface lessons recorded for successor caps. |
| **102** | novel-lifecycle-cluster | **Shipped** | Foundation child of Spec 101: consolidated novel `OntologyExtension` + 14 base lifecycle verbs (idea capture → novel/chapter/scene CRUD → status flips → progress aggregate → resume session) + 10-phase `novel-concept` walkable skill + StateDriver protocol delta. | **Slices 1+2 SHIPPED** (PR #80 + extension): Slice 1 = 5 verbs (capture_idea, promote_idea, list_ideas, find_novel, set_novel_status) + Idea node + IDEA_STATUS enum + PROMOTED_TO edge + 10-phase skill extension. Slice 2 = 6 verbs (list_chapters, create_scene, set_chapter_status, rename_novel, novel_progress, resume_session) + Scene node + SCENE_POV enum (first/second/third-limited/third-omniscient) + SCENE_OF edge + `_require_chapter` helper. 34 lifecycle tests (17 Slice 1 + 17 Slice 2). Graph-only; StateDriver Protocol deferred to a Spec-115-equivalent follow-up. **Still:** per-cluster file split; the StateDriver Protocol; iteration-6 import/export verbs (Wave-4 PR-K2). |
| 116 | sqlmodel-data-layer | **Draft** | Refactor music capability's structured-data persistence to Python SQLModel (Pydantic + SQLAlchemy). Replaces raw SQL in `SqliteDBDriver` + JSON side-cars in `FileStateDriver` + in-memory dicts in `FakeStateDriver` with one typed ORM layer. Two-store doctrine: graphqlite owns provenance edges; SQLModel owns domain data. `sqlmodel` as core dep (per user directive 2026-06-09). Backwards-compatible (Protocol surfaces unchanged). | Spec authored; waiting on Spec 115 merge before implementation. |
| 120 | novel-storyform-completion | **Shipped** | Closes the Dramatica engine. `_resolve_term(term_id)` kind-agnostic lookup (resolves `el.self-interest` → `var.self-interest`, `t.past` → `type.past`). 8 new decidable checks (rows 1, 2, 3, 6, 7, 8, 9, 10): pair-reciprocity, ktad-coverage, quad-completeness, crucial-element-placement, resolve-outcome-judgment, approach-concern (WARN), mental-sex-problem-solving, signpost-permutation. `novel_coherence_check(ncp)` composite with check-chaining: row 5 gates 3+10; row 10 gates 2 (the Slice-2 retraction lesson made structural). `storyform-build` 6-phase walkable skill with verb-bound phases. **Exact-fail contract holds across ALL 11 fixtures** under the composite — parametrized assertion. Composite records `Artefact(kind="storyform-coherence-report")` with SERVES edge — provenance moat preserved. | 35 new tests green; 275 across novel/naming/install/analyze green; lint clean (docstring briefs tightened); check-drift clean. |
| 121 | novel-production-binding | **Shipped (Slice 1)** | NovelConfig 4-level resolution (`.agency/novel-config.yaml` → `~/.agency-novel/` → `$AGENCY_NOVEL_HOME` → defaults) with mtime-cached `load()` + idempotent `bootstrap()`. FileNovelStateDriver writes prior-spec-010 layout `works/{author}/works/{genre}/{slug}/` with `work.md`, `chapters/NN-slug.md`, `scenes/`, `ncp.json`; render fidelity (F3 template field substitution + F4/F5 frontmatter `status` round-trip) from day one. Lazy auto-wiring mirrors Spec 117: `_NOVEL_DRIVER_NAMES`, `_production_enabled`, `_autowire_novel_drivers`, `_require_drv`; bare unit-test engines keep typed DEPENDENCY_MISSING. `create_novel(title, author, genre)` + `create_chapter` gain opportunistic disk side-effects when driver wired. MCP entrypoint sets `engine._novel_production = True`. **Slice 2** (deferred): `import_from_markdown` + `export_for_editor` + `reconcile_disk_with_graph`. | 14 new tests + 235 across novel/naming/install green; drift clean. |
| 127 | dynamic-prompt-assembly | **Shipped (Slice 1)** | `prompt.assemble_scene_brief(scene_id, max_tokens=4000, section_budget=320)` walks Scene → Chapter → Novel → Storyform and composes a bounded prompt with all 7 sections (storyform / pov_card / scene_cast / world_rules / continuity / foreshadowing / voice_constraints). Storyform section composes via Spec 129's `fragments_for`; sections with future-spec dependencies (132/123/128) ship as **explicit placeholders that name the gating spec** (not silent gaps). Section budget truncates with `...` flag; total budget drops lowest-priority sections. Records `Artefact(kind="scene-brief")` + SERVES intent; `sources` array tags each contributing node with `contributed: <section>`. Renders as structured markdown. | 16 new tests + 250+ across prompt/novel/naming green; drift clean; surface_size accept-warn marker added to prompt cap with rationale. **Slice 2**: `assemble_chapter_brief`, `BRIEF_USED` edges, `novel.render_chapter_brief` wire-up, scene-writer 5-phase skill. |
| 129 | dramatica-prompt-fragments | **Shipped (Slice 1)** | Bootstrap **34 hand-authored fragments** (4 throughlines + 4 classes + 16 types + 8 archetypes + 2 elements) shipped in `fragments.json`; vendored bootstrap + per-project YAML overlay (`.agency/dramatica-fragments-overlay.yaml`); `prompt.fragment(slug)` single lookup with kind-prefix alias resolution via Spec 120's `_resolve_term`; `prompt.fragments_for(scope, max_tokens=2000)` multi-entry composer (throughline + class + concern + crucial + problem + solution + archetypes; order matters when budget binds); `prompt.register_fragment(slug, text)` runtime-extensible overlay write with cache invalidation. **Slice 2** (deferred): authoring the remaining ~270 ontology entries' fragments + lint enforcement of "fragment OR placeholder". | 15 new tests + 235 across prompt/novel/naming/install green; drift clean. Unblocks Spec 127 (assemble_scene_brief composes the brief's storyform section via `prompt.fragments_for`). |
| 122 | novel-editorial-pipeline | **Shipped** | 5 chapter-spanning prose verbs (`check_voice_consistency` z-score outlier detection, `check_pov_consistency` via `ctx.neighbors` SCENE_OF, `check_continuity` proper-noun registry + Levenshtein close-pair scan, `check_sensitivity` advisory lexicon, `chapter_report_full` editorial-dashboard aggregate) + 3 composite editorial-stage gates (developmental → line → copy mirroring music's lyric-gate progression) + 2 walkable skills (`developmental-editor` 5-phase, `line-editor` 4-phase; both with verb-bound phases per Spec 080 doctrine). Phase 2 of developmental-editor binds to `novel_coherence_check` (Spec 120). Exact-severity discipline: `check_sensitivity` always passes (advisory); structural failures gate. | 17 new tests + 252 across novel/naming/install green; drift clean. Open Q1 resolved: `z > 2.0` documented tunable. Open Q2 (genre readability bands) deferred to a follow-up; current `copy_gate` warns outside 50-90 Flesch but never blocks on readability. |
| 123 | novel-worldbuilding-psychology | **Shipped (Slice 1)** | World sub-graph shipped: 6 new ontology nodes (World/Culture/Religion/Language/MagicSystem/WorldAxiom) + 2 new edges (PART_OF_WORLD, CONTRADICTS) + 5 character-to-world relationship edges (BELONGS_TO/INHABITS/WORSHIPS/SPEAKS/WIELDS) + WORLD_AXIOM_SEVERITY enum + 9 verbs (`create_world`/`create_culture`/`create_religion`/`create_language`/`create_magic_system`/`create_world_axiom`/`list_world` tree via `ctx.neighbors` PART_OF_WORLD/`find_axiom_contradictions` decidable scan with negation-XOR + motif-word overlap/`link_character_to_world` edge-kind whitelist). **Slice 2** (deferred): Character psychology (PsychProfile big-five/enneagram/ifs/jung + Trait + `validate_dramatica_mapping` — note Spec 138 supersedes the PsychProfile carve-out with the plural-character system); Conflict + Theme tracking; PlantedElement foreshadowing (Chekhov's-gun report); skill rewiring (`character-architect`/`world-bible-architect` phase bindings); `developmental_gate` extension. | 14 new tests + 249 across novel/naming/install green; drift clean. Open Q1: novel-scoped Conflict/Theme. Open Q2: decidable v1 (negation-XOR) shipped; judgement pass via `thinking.red_team` xcap deferred. |
| 124 | novel-format-driver | **Shipped (Slice 1)** | FakeFormatDriver (deterministic content-keyed paths, zero binaries — mirrors FakeAudioDriver) + 3 export verbs (`export_epub` / `export_pdf` / `export_docx`) + `publication_gate` terminal composite + `publish-prep` 4-phase walkable skill (render → export → publication-gate → sign-off, with verb-bound phases). `production_drivers` bundle extended with `novel_format`; `_maybe_format_driver` opportunistic helper. Each export records `Artefact(kind="published-manuscript")` with SERVES + PRODUCES edges. Bare engine path gives typed DEPENDENCY_MISSING. **Slice 2** (deferred): `PandocFormatDriver` (weasyprint per Open Q1) + `[novel-format]` extra + back-cover template port. | 12 new tests + 247 across novel/naming/install green; drift clean; lint clean. |
| 125 | graph-neighbors-helper | **Shipped** | Substrate `ctx.neighbors(node_id, edge, direction="in", limit=100)` on CapabilityContext — one Cypher hop, returns property-dict lists (same shape as `find()`). Closes the Round-1 F4 advisory: **10 novel call sites migrated** (all `find("Chapter")`+filter scans replaced by `neighbors(novel_id, "CHAPTER_OF")`); behaviour-preserving (141 novel tests green, parity test asserts find+filter and neighbors return the same set). Music AlbumClaim scans audited + deferred — they filter by status (`verified=="pending"`), not parent edge; no `neighbors()` migration applies. CLAUDE.md dormant-surface heuristic extended: "declare an edge ⇒ traverse it via ctx.neighbors". | 9 substrate tests + 141 novel regression green. ENGINE edit (one method, ~30 LOC) justified per drop-in-bar exception — the gap was forcing caps to bypass edges. |
| 126 | research-ingest-gdoc | **Shipped** | `research.ingest_gdoc(source, dest)` + `research.record_ingested_source(...)`: subagent-isolated Google Drive → Markdown ingest. The verb composes a dispatch contract (prompt + 4 tools + dest + callback) — NO I/O, NO MCP dependency at verb layer; the doc body never crosses back to orchestrator context because the contract's tool budget excludes Read/Grep. Sibling verb records `Artefact(kind="ingested-source")` with SERVES + PRODUCES edges; idempotent on `(intent_id, sha256)`. Unblocks large-corpus ingestion for novel research + research-report reading. | 18 tests green; check-drift clean. Zero engine edits — drop-in-capability bar respected. |
| 117 | music-runtime-binding | **Shipped** | Runtime binding for the music capability — closes the dogfooded gap that Spec 115's production drivers were unreachable from the MCP runtime. Lazy auto-wiring: `MusicCapability._require_drv` builds `production_drivers(MusicConfig.bootstrap())` ONCE on first driver miss. **Slice 2 (render fidelity, F3/F4/F5):** track template `status: "Not Started"` (F5); `_fill_album_body`/`_fill_track_body` substitute title/artist/genre/track-# (F3); `FileStateDriver.update_track_field` edits frontmatter in place (F4). **F6** name-exposure enforcement → Spec 119 (Shipped). All slices closed. | F1–F6 closed (F6 via Spec 119). Spec 115 Slice 2 (real audio/cloud drivers) is a separate Spec, not a 117 follow-up. |
| 119 | music-name-exposure | **Shipped** | F6 from Spec 117: project-DNA name-exposure enforcement. `MusicConfig.name_exposure_blocklist` (from `name_exposure.blocklist`, default `[]`); `check_name_exposure(text, roster=None)` transform verb (driver-free, whole-word + case-insensitive — "Lex" never fires inside "lexicon") + `name_exposure_gate` effect gate (mirrors `explicit_gate`; no-op pass on empty roster) sharing `_name_exposure_hits`. Composed as the additive 5th sub-gate in `lyrics_pregen_gate`. **Slice 2:** `promo_review` scans copy too (name hit → `name_exposure` fail finding + score penalty → `promo_review_gate` blocks). 13 new/updated tests; verb-count interop tests de-pinned to invariants (rule 8); check-drift clean. | **Shipped.** F6 closed across BOTH public text surfaces (lyrics + promo). Only the design/art layer remains a documented non-goal. |
| 114 | plugin-as-session-driver | **Shipped** | The plugin IS the primary session driver: SessionLifecycle + ModeShift + DecisionRecord + SessionReflection. **Slice 1 SHIPPED**: 6 verbs + 5-phase walkable skill + 17 tests. **Slice 2 SHIPPED**: `develop.session_resume(for_intent_id)` cross-session handoff with mode-aware `suggested_action` + 7 tests. Hooks/session-start integration delivered via Spec 195 (BoundaryUse capture). | Remaining brainstorm/write_spec/implement skill bodies tracked by Spec 229 (session-driver-slice2-hooks, wave-8 enhancement). |
| 109 | prompt-capability | **Shipped** | Prompt-engineering capability: research-dossier lineage + engineering lineage. **Slice 1 SHIPPED**: 9 verbs + 2 walkable skills + 2 templates + 9 nodes + 7 closed enums + 6 edges + 4 schemas + 23 tests. | **Slice 2** (7 more verbs + 3 more skills + YAML catalog + anti-pattern library + cross-cap delegation) tracked by Spec 225 (prompt-cap-slice2-llm, wave-8 enhancement). |
| 110 | thinking-capability | **Shipped** | Critical-thinking capability: **Slice 1 SHIPPED**: 11 verbs (8 founding + 2 net-new + 1 composite) + 1 walkable skill (`critical-thinking-pass`) + 2 schemas + 2 nodes + 14 tests. | **Slice 2** (4 more methods + 2 composites + 2 skills + 7 templates + intent migration per Spec 111) tracked by Spec 226 (thinking-cap-slice2-wet, wave-8 enhancement). |
| 128 | story-time-graph | **Shipped** | `StoryTimeEvent` + `NarrativeBeat` nodes + 3 new edges (HAPPENS_AT / REVEALED_IN / PRECEDES) + 6 verbs (`record_story_event`, `reveal_in_scene`, `list_story_events_up_to`, `list_reveals_in`, `mark_narrative_beat`, `narrative_order`). Story-time as plain string (max flexibility; lex sort). **Spec 127 upgrade**: `_compose_continuity` now reads `list_story_events_up_to(scene_id)` and bullet-renders the events (`[when_story] label`); placeholder text retired. `_BriefContext` extended with `_ctx` so composers can spawn sibling caps. Open Q1: plain string. Open Q2: HAPPENS_AT edge (no Scene.story_time property — scenes can depict multiple events). | 16 new tests + 294 across novel/prompt/naming/install green; drift clean; lint clean. Unblocks Spec 131 (NarrativeBeat ordering for `what_does_X_know_as_of`). |
| 130 | scene-writer-skill | **Shipped (Slice 1)** | `scene-writer` 5-phase walkable skill registered on novel ontology: (1) assemble → `prompt.assemble_scene_brief`; (2) validate-constraints (no bound verb — brief shape is the gate); (3) generate (no driver binding yet — Slice 2 territory gated on Spec 005); (4) check → 4 verbs (`check_filter_words` / `check_dialogue_attribution` / `check_show_dont_tell` / `novel_coherence_check`); (5) integrate (**HARD GATE**) → `novel.integrate_scene_body` (writes body to Scene + records `Artefact(kind="scene-integration")` with SERVES + PRODUCES edges). **Slice 2** (deferred): FakeTextDriver fixture + production TextDriver (Spec 005); phase 2 validation verb; auto-update LEARNED_IN ledger on phase 5. | 9 new tests + 303 across novel/prompt/naming/install green; drift clean. **Closes the dynamic-prompt depth wave** — Spec 127's 7 brief sections are all graph-backed and the walkable loop ties them together. |
| 131 | character-knowledge-ledger | **Shipped** | `KnownFact {character, fact}` node + `KNOWS` (Character → KnownFact, one-hop via `ctx.neighbors`) + `LEARNED_IN` (KnownFact → Scene) edges + 3 verbs: `record_character_learns` (mints + edges + SERVES intent), `what_does_X_know_as_of` (filters by LEARNED_IN scene's chapter ≤ target chapter), `flag_anachronistic_reference` (returns `{anachronism, expected_learned_in}` or `{anachronism: False, no_record: True}`). **Spec 127 upgrade**: `_compose_pov_card` reads optional Scene.pov_character_id and adds "POV knows" subsection with bullet-rendered facts when set. Narrative-position is approximated by chapter.number (NarrativeBeat-based ordering is Slice 2 refinement). character_id accepts any node id (Character node lands in Spec 123 Slice 2). | 13 new tests + 307 across novel/prompt/naming/install green; drift clean. Open Q1: forgetting/unlearning deferred. Open Q2: no embedding-store indexing. |
| 132 | codex-entity-tracking | **Shipped** | `CodexEntry {novel, slug, name, kind}` node + CODEX_OF edge + `CODEX_ENTRY_KIND` enum (location/minor-character/artefact/concept/faction) + 5 verbs: `create_codex_entry` (triggers default to `name, slug`), `list_codex_entries` (kind filter; skips archived), `match_codex_entries` (case-insensitive substring scan over comma-separated triggers; archived skipped), `update_codex_entry`, `archive_codex_entry` (soft delete). **Spec 127 upgrade**: `_compose_world_rules` scans chapter body + title + scene slug + scene cast against registered triggers via `match_codex_entries`, renders `- **Name** (kind): body` bullets, retires placeholder. `_BriefContext._ctx` added + `novel.id` injected. | 17 new tests + 295 across novel/prompt/naming/install green; drift clean. Open Q1: per-novel codex (CODEX_OF → Novel). Open Q2: all kinds auto-trigger. Slice 2 carve-out: word-boundary regex matching (current is plain substring). |
| 133 | story-structure-templates | **Draft** | Save the Cat (15-beat) + Three-Act + Hero's Journey (12-beat Campbell) + Story Circle (Harmon 8-beat) + Snowflake (Ingermanson expansion). Vendored JSON templates + `StructureTemplate` lookup + `BeatExpectation` ontology node + `FULFILS` edge + 6 verbs (`list_structure_templates` / `get_structure_template` / `apply_structure` / `anchor_beat` / `check_structure_coverage` / `structure_position_report`). `actual_position` derived from cumulative word count or chapter midpoint; flags beats `|actual-target| > 0.10` as out-of-position. `storyform-build` skill (Spec 120) optionally chains a template-pick phase. | Spec authored 2026-06-10. Pacing complement to Dramatica's meaning engine — Dramatica answers what each scene means, structure answers where it lands. |
| 134 | pov-voice-profiles | **Draft** | `VoiceProfile {vocabulary_floor, sentence_avg_target/stddev, taboo_words, signature_phrases, formality_target, contractions}` + VOICE_OF edge (one per Character, overwrite-on-update) + 6 verbs (`create_voice_profile` / `update_voice_profile` / `get_voice_profile` / `score_voice_match` / `check_pov_voice` / `voice_drift_report`) + `voice_drift_gate(novel_id, min_score=70)` composite for the editorial pipeline. Scene-writer phase 4 extended with `novel.check_pov_voice`. Defaults auto-derived from 5+ already-drafted scenes (computed, not snapshot — rule 8). Spec 138 extends with the per-alter plural layer. | Spec authored 2026-06-10. Closes the "every POV character sounds the same" failure mode. Soft severity throughout — voice drift is a warning, not a hard gate (except `voice_drift_gate` when explicitly chained). |
| 135 | sensitivity-reader-workflow | **Draft** | `SensitivityFinding {scene, category, severity, finding, suggestion, status, reader_id, reader_notes}` + FLAGGED_IN edge + 3 enums (SENSITIVITY_CATEGORIES open-set / SENSITIVITY_SEVERITY 4-level / SENSITIVITY_STATUS 4-state) + 7 verbs (record / list / update_status / acknowledge / dismiss / mark_revised / sensitivity_pass_gate). `sensitivity-reader-pass` 4-phase walkable skill (scan → review → revise → sign-off hard gate). Scan-findings auto-flag at `info` severity from Spec 122's `check_sensitivity` lexicon; human reader elevates. Dismissal requires a non-empty reason — no silent ignores. | Spec authored 2026-06-10. Turns the advisory lexicon into a tracked workflow with provenance moat. Every finding IS a graph node; resolution status is queryable. |
| 136 | dual-storyform-architecture | **Draft** | Post-Dramatica: `StoryformSet` + N simultaneous storyforms + `check_klein_c_inversion` + `StoryformTransition` (Vortex) + per-chapter dual-POV routing + `bridge_frequency_report` + `dual_storyform_coherence_check`. Derived from the Kohärenz-Protokoll dual-storyform A‖B. | KP wave (post-ingest 2026-06-10). The biggest gap: the engine assumed one storyform; KP needs two with Klein-c inversion symmetry. |
| 137 | canon-provenance-locks | **Draft** | `CANON_STATUS` enum ([K]/[V]/[S]/[L]) as a cross-cutting marker + `Lock` node + LOCKS edge + verbs (set_canon_status / record_lock / lock_index / resolve_canon_conflict / quarry_filter / canon_audit) + `canon-gate`. Source-hierarchy = newer-wins. | KP wave. The source-of-truth discipline the whole project runs on. |
| 138 | plural-character-system | **Draft** | `CharacterSystem` + `Alter` (ANP/EP/special/mirror categories + trauma-layer) + `PHOBIA_OF` conflict-matrix edge + verbs (create_system / add_alter / record_alter_conflict / conflict_matrix_report / assign_voice_to_alter / check_alter_recognition / switching_log / validate_no_fusion) + anti-cliché taboo rules. Extends 123 Slice 2 + 134 with the plural container. | KP wave. The dissociative-system (13-alter) model the protagonist needs. |
| 139 | reveal-discipline | **Draft** | `AUDIENCE_TIER` (reader/pov/antagonist) + `RevealRule` + `GOVERNS_REVEAL` + verbs (set_reveal_rule / check_reveal_timing / reveal_timeline_report / check_veil / record_leerstelle / reader_function_audit) + `reveal_gate`. Reader-steering / multiplicity-veil. Complements 131 (in-world knowledge) with authorial reveal-timing. | KP wave. "What may the reader know when" across three audience horizons. |
| 140 | project-rulesets-motifs | **Draft** | `DEFECT_SEVERITY` + `ProjectRule` (4 decidable predicate-kinds: mutual-exclusion / per-scene-budget / forbidden-verbatim / register-forbidden) + verbs (register/list/run_project_rules + `project_rule_gate`) + `Motif`/`ECHOES_IN` + `Anchor`/`PLANTS`/`PAYS_OFF`. Author-authored R-rules with severity tiers + motif-echo budget + foreshadowing-anchor tracking. | KP wave. Turns generic prose-lint into a project-extensible self-review rule-set (the highest-frequency KP operation). |
| 141 | chapter-briefing-mode-blocks | **Draft** | `ModeBlock` + `IN_MODE_BLOCK` + verbs (define_mode_block / assign_chapter_to_block / mode_block_report / check_mode_vs_storyform_boundary / check_genre_bleed) + 13-section chapter-briefing template + `render_chapter_briefing` (aggregates the 136–140 stack) + `briefing_checklist`. Mode ≠ storyform-boundary made machine-checkable. | KP wave. The chapter-level briefing bridge + narrative-mode-block + genre-per-act layer. |
| 142 | novel-craft-skill-walks | **Draft** | Six walkable skills mapping to the 136–141 verb clusters (`dual-storyform-author` / `canon-lock-author` / `alter-roster-builder` / `reveal-rule-author` / `r-rule-author` / `chapter-briefing-author`), each with one HARD GATE phase; `develop.skill_walk` gains a `--dry-run` flag. Turns scattered verbs into named operations an LLM driver can drive end-to-end. | KP wave deep skills (2026-06-10). The authoring-loop scaffolding 136–141 imply but don't ship. |
| 143 | kohaerenz-prompt-fragments | **Draft** | ~60 KP-derived prompt fragments across 6 families (plurality / klein-c / mode-block / reveal-tier / R-rule / synthesis-movements) vendored as `kp-fragments.yaml`; extends Spec 129 `prompt.fragments_for_scope` + new `prompt.compose_drafting_brief(scene_id)`. Project-overlay-able; loads only when KP nodes detected. | KP wave deep prompts. The LLM-side vocabulary the 136–141 graph surface needs. |
| 144 | voice-locked-drafting-prompt | **Draft** | `prompt.compose_voice_locked_brief(scene_id, alter_id)` bakes Alter + VoiceProfile into a single drafting brief with non-truncatable §TABOO block + §EXAMPLES exemplars; co-front guard refuses max-pair PHOBIA_OF scenes unless overridden; `voice_drift_audit` defensive check; scene-writer phase-1 chains it when an alter is bound. | KP wave deep prompts. The Sprach-DNA → one-shot prompt path (the per-scene "recognized not named" discipline made productive). |
| 145 | novel-preflight-composite-skill | **Draft** | `novel-preflight` 5-phase walkable: briefing-ready · canon-clean · reveal-clear · r-rules-dry-run · voice-ready (HARD GATE). Read-only; <200ms on 40-chapter graph. `novel.preflight_report(scene_id)` non-walk variant; editorial-gate (Spec 122) short-circuits on critical findings; scene-writer (Spec 130) gains phase-0 preflight default-on when KP nodes present. | KP wave deep skills. The daily-driver pre-scene checklist that composes 137/139/140/141/144 audits into one verdict. |

## Suggested implementation order (next 5)

Refreshed 2026-06-11 (post Wave-1 Slice 1 wave: 5 chain anchors + 3 substrate enhancements landed; 152 + 154 in flight). Ranked by leverage — finishing the chain anchors unblocks the rest of the Vision Charter.

1. **Spec 149 Slice 2** — `scripts/derive-docs` (TODO/matrix/SkillDoc/Followup auto-derivation). Single highest-leverage move: unlocks the 129-baseline `vision_goals:` backfill, makes the alignment matrix self-updating, and is a prerequisite for Spec 261's stop-condition verification.
2. **Spec 147 Slice 2** — Managed-Agents bridge (`dispatch_session`). Turns 150/148/177's "stub" paths into real fan-out; prerequisite for any "real LLM driver does the work" slice.
3. **Spec 146 Slice 2** — `_check_response_prefix` AST lint rule. Promotes prefix discipline to a CI gate per Spec 058 WARN→error doctrine.
4. **Spec 150 Slice 2** — swap keyword classifier for AnthropicDriver structured-output classification (depends on 147 Slice 2).
5. **Spec 191** — vision-alignment live matrix. Derives goal coverage from `vision_goals:` frontmatter (depends on 149 Slice 2); unblocks every retrospective.

## When to update this file

Update THIS file (TODO.md) in the SAME PR / commit when:

1. A spec ships (Not started / Partial → Shipped). Move row's Status
   column; bump verdict-counts at top.
2. A spec's implementation makes meaningful progress (Not started →
   Partial; document the partial scope in `Plan/NNN-…/spec.md`'s
   Followup section).
3. A new spec gets a folder under `Plan/`. Add a row here.
4. A spec is closed (deferred / superseded / cancelled). Note in row's
   "Blocker / Next step".
5. A spec is renumbered (rare; happens on naming collision after a
   merge — see 2026-06-02 history where 031-038 became 039-046).

Per-spec deep state (test counts, file:line evidence, verbatim
"Done / Still-to-implement / Refinement needed") lives in each
`Plan/NNN-…/spec.md`'s `## Followup — Implementation Status (…)` section.
This file rolls up; that section grounds.

## Pointers

- Per-spec details: `Plan/NNN-slug/spec.md` § Followup section.
- Historical overview: `Plan/000-overview.md`.
- The doctrine drives implementation order: see CLAUDE.md Rule §
  "Decide before dispatching" — do not parallelise spec implementation
  unless dependencies are crisp.
