# Spec 153 Slice 2 — schema-coverage baseline (Spec 054 drift pattern).
# Enumerates KNOWN-uncovered ontology labels in agency/.
# The Slice 2 gate flags REGRESSIONS only: a new label here means
# either author a schema for it OR add to this baseline.
#
# Format: one label per line
#
# Spec 153 Slice 6 Wave 6 reached coverage 1.0; the Spec 133-141 KP wave
# (novel structure/voice/dual-storyform/canon/plural/reveal/rulesets/
# mode-blocks) added 14 graph-only node types below — schema backfill is a
# named follow-up of that wave (author schemas, then shrink this list).
Alter
AlterConflict
Anchor
BeatExpectation
CharacterSystem
Leerstelle
Lock
ModeBlock
Motif
ProjectRule
RevealRule
StoryformSet
StoryformTransition
VoiceProfile
