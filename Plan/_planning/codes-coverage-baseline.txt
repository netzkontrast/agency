# Spec 151 Slice 2 — codes-coverage baseline (Spec 054 drift pattern).
# Enumerates KNOWN historical STRING_LITERAL ToolResult.failure offenders
# in agency/. The Slice 2 gate flags REGRESSIONS only: a new entry here
# means the literal must be migrated to a Codes.X member (Spec 151 Slice 4)
# or its appearance justified + added below.
#
# Format: <relative-path>:<line>:<literal>
#
