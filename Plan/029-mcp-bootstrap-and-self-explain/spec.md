---
spec_id: "029"
slug: mcp-bootstrap-and-self-explain
status: draft
owner: "@agency"
depends_on: ["020", "023"]
affects:
  - agency/engine.py                # add intent_bootstrap / agency_install / agency_welcome substrate tools
  - agency/install.py               # CLAUDE.md snippet on scaffold + helpers reused as MCP-callable
  - agency/capability.py            # better SERVES-guard error message ("call intent_bootstrap")
  - agency/capabilities/reflect.py  # surface scope enum values in the verb docstring (F4)
  - agency/cli.py                   # mention intent_bootstrap in usage (parity)
  - tests/test_mcp_bootstrap.py     # NEW — RED tests for the substrate tools
  - tests/test_welcome.py           # NEW — RED tests for welcome shape
  - tests/test_install_verb.py      # NEW — RED tests for the install MCP verb
  - skills/help/SKILL.md            # regenerated by python -m agency.install
estimated_jules_sessions: 0
domain: substrate
wave: 4
---

# Spec 029 — MCP bootstrap + self-explain (close F1/F2/F4/F6)

## Why

The KP-Novel-Decomposition session
(`Plan/AGENCY-PLUGIN-FEHLERBERICHT.md` in the
`kohaerenzprotokoll` repo) surfaced a hard discovery loop:

> Every capability verb requires a valid `intent_id`. Only
> `engine.intent.capture()` mints one. That method is NOT wired as
> an MCP verb. The single bootstrap path is the bash CLI subcommand
> `python -m agency.cli intent …`.

In MCP-only harnesses (the documented normal case) the agent walks
into the substrate, hits the SERVES guard with a fresh repository,
and has nowhere to go without reading source. The dogfood session
escaped via the bash side-pipe — but that side-pipe is also where
silent-failure F5 lives (system `python3` without deps writes
nothing).

Same session: the agent burned tokens re-discovering how to bootstrap,
how to bypass stale `JULES_API_KEY`, what values `scope` accepts, and
where `.agency/` should live in a target repo. The plugin is not
self-explanatory enough — the moat (the graph) is wrapped in
discovery friction every new caller pays in tokens.

Two requirements collapse into one design:

1. **MCP-native bootstrap.** A pure MCP client must be able to mint
   an Intent, install the `.agency/` scaffold into the current target
   repo, and run its first capability verb — without dropping to bash.
2. **First-contact onboarding.** A fresh agent should reach the
   substrate via a single welcome call that returns: the discovery
   surface, the bootstrap example, and the canonical first call —
   not by reading three doc files.

The combined effect is **token-efficient self-explanation**: one
discovery call replaces three file reads, error messages teach the
next call rather than dead-end, and the install verb makes Mode B
(plugin installed in a target repo) safe to invoke from inside MCP.

## Actors (Cockburn)

The substrate tools serve three distinct caller states. Welcome behavior must be correct for each:

- **A1 — Fresh client on a fresh target.** No `.agency/` dir, no `session.db`, no Intent.
  Welcome MUST resolve `db_path` to the *would-be* path (per Spec 020 resolution
  order); a missing DB file is not an error here.
- **A2 — Fresh client on an installed target.** `.agency/` exists but the graph
  is empty (no Intent yet). Welcome behaves as A1 but `db_path` exists.
- **A3 — Returning client.** Intent(s) in the graph; agent is rediscovering tools.
  Welcome is purely introspective — no graph writes regardless of state.

## Done When

### A. Engine-substrate MCP tools (`engine.py::build_mcp`)

Three tools added alongside `lifecycle_gate` / `memory_graph_provenance`
— same exception class (substrate tools, no `intent_id` required for
the bootstrap pair; `agency_welcome` is pure introspection):

- [ ] **`intent_bootstrap(purpose, deliverable, acceptance) → dict`**
  - Mints AND confirms an Intent in one call (the same operation
    `bin/agency intent` runs).
  - Returns `{intent_id, status: "confirmed", next: "<example code>"}`
    where `next` is a copy-pasteable `call_tool(...)` example for the
    next call.
  - The ONLY substrate verb that does not require `intent_id` — same
    exception class as `bin/agency intent`.
  - Records the Intent under the graph DB resolved by Spec 020
    (`AGENCY_DB` env → `./.agency/session.db`).
  - **Error contract (Wiegers/Nygard).** On empty/missing required
    field, raises `ValueError` with the field name in the message
    (e.g. `"intent_bootstrap: 'purpose' must be non-empty"`). FastMCP
    surfaces this as a tool result with `is_error=true` and the
    message in `content[0].text` — verified by
    `test_intent_bootstrap_rejects_empty_required_fields`.
  - **No Invocation recorded (Crispin).** Bootstrap matches CLI
    behavior — it bypasses `Registry.invoke`, so no `Invocation` node
    is created and no self-loop SERVES edge appears. Asserted by
    `test_bootstrap_records_no_invocation` (Intent count == 1,
    Invocation count == 0 after a single call).
  - **Evolution doctrine (Newman).** New params are added with
    sensible defaults (`tags=None`, `parent_intent_id=None`, …); the
    wire-name `intent_bootstrap` never changes. No versioned tools,
    no `kwargs` dict. Matches the per-capability auto-wire convention.
  - **Given/When/Then (Adzic):**
    ```
    Given: an Engine over an empty graph DB
    When:  intent_bootstrap(purpose="X", deliverable="Y", acceptance="Z")
    Then:  result.intent_id starts with "intent:"
           result.status == "confirmed"
           result.next contains "call_tool(" and the literal intent_id
           memory.find("Intent") == [one row with status=confirmed]
           memory.find("Invocation") == []
    ```

- [ ] **`agency_install(target=None) → dict`**
  - Wraps the existing `install.scaffold_db(root)` + writes a
    `CLAUDE.md` snippet at the target root if absent.
  - `target` default = `CLAUDE_PROJECT_DIR` env → cwd (mirrors
    `_scaffold_target`).
  - Idempotent — re-running on a populated tree is a no-op for any
    file already present.
  - Returns `{target, scaffolded: [<paths>], gitattributes_updated,
    claude_md_path, claude_md_updated, next: "<call_tool example>"}`.
    `claude_md_path` is the LITERAL absolute path written (so the
    caller can verify); `claude_md_updated` is True only when content
    actually changed.
  - The CLAUDE.md snippet is a SHORT block (≤ 40 lines) that names:
    - `mcp__plugin_agency_agency__execute` as the first call;
    - `agency_welcome` as the canonical onboarding tool;
    - `intent_bootstrap` as the way to mint an intent;
    - the `.agency/session.db` location (committed to git per Spec 020).
  - If a CLAUDE.md already exists, INSERT or REPLACE the snippet
    bounded by explicit markers
    `<!-- agency:onboarding:start --> … <!-- agency:onboarding:end -->`.
    Re-running REPLACES only between markers — user content outside is
    never touched. Idempotent on re-run with the same content.
  - Fails loud on a non-writable target with a clear OSError-wrapped
    message naming the path.
  - **Given/When/Then — fresh target (Adzic, A1 actor):**
    ```
    Given: an empty tmp_path directory
    When:  agency_install(target=tmp_path)
    Then:  tmp_path/.agency/.gitkeep exists
           tmp_path/.agency/README.md exists
           tmp_path/.gitattributes contains the Spec 020 binary block
           tmp_path/CLAUDE.md exists and contains the marker pair
           result.claude_md_updated is True
    ```
  - **Given/When/Then — populated target (Adzic, A3 actor):**
    ```
    Given: tmp_path with all of the above already present
    When:  agency_install(target=tmp_path)
    Then:  no file content changes
           result.claude_md_updated is False
           result.scaffolded list contains no new entries
    ```

- [ ] **`agency_welcome() → dict`**
  - One-shot onboarding payload. No `intent_id` required.
  - Returns `{bootstrap_example, install_example, capabilities, db_path,
    next: <ordered list of 3 calls>}` where:
    - `bootstrap_example` — string with the exact `intent_bootstrap`
      call to make next;
    - `install_example` — string with the exact `agency_install` call;
    - `capabilities` — `{capability: [verb, ...]}` (same shape as
      `plugin.help_map`, deterministic order);
    - `db_path` — resolved DB path (so the agent knows where state
      lives);
    - `next` — ordered list `["agency_install", "intent_bootstrap",
      "search"]` with one-line guidance per step.
  - **A1/A2/A3 invariance (Cockburn).** Welcome is pure introspection;
    it makes NO graph writes regardless of caller state. `db_path`
    resolution must not require the DB file to exist (Spec 020
    `resolve_db_path(None)` returns the *would-be* path on A1).
    Verified by `test_welcome_works_on_fresh_target`.
  - Token budget: full payload ≤ 1 KB on a 20-capability registry
    (verified by `tests/test_welcome_token_budget.py`).

### B. Self-explanatory error messages

- [ ] **SERVES-guard error** (`capability.py::Registry.invoke`)
  - Replace `ValueError(f"intent_id {intent_id!r} is not an Intent node")`
    with a message that names the bootstrap call:
    `"intent_id {…} is not an Intent node. Mint one with
    `intent_bootstrap(purpose=…, deliverable=…, acceptance=…)`
    (MCP) or `python -m agency.cli intent …` (bash)."`
  - Test: `test_serves_guard_error_mentions_bootstrap`.

- [ ] **Empty-graph `search` preamble**
  - When `search(query=…)` returns no hits AND the Intent count in
    the graph is 0, prepend a small `_bootstrap_hint` to the result:
    `"No intents yet — start with `intent_bootstrap` or
    `agency_welcome` for the full first-call payload."`
  - Verified by `test_search_empty_graph_emits_hint`.

### C. Enum-domain disclosure (F4)

- [ ] **`reflect.note` / `reflect.batch_note` docstrings**
  - Append `Allowed scopes: observation, project, reflection,
    technical, user, world.` to both docstrings so the brief slice
    AND `get_schema` show the enum domain inline.
  - Verify the enum domain stays sourced from `REFLECT_SCOPES`
    (single source of truth — docstring is rendered from the set).

### D. Help skill regeneration

- [ ] Running `python -m agency.install` regenerates
  `skills/help/SKILL.md` so the welcome example is present in the
  Skill-loader path too.
- [ ] The skill body MUST reference `intent_bootstrap` and
  `agency_install` and `agency_welcome` as the MCP first-call surface
  (no bash-only bootstrap as the only path).
- [ ] Tracking-issue language in the current `_MCP_QUICKSTART`
  ("add a capability_intent_capture verb so MCP-only sessions can
  self-bootstrap") is REMOVED — the spec ships its replacement.

### E. Isomorphism & shared logic

- [ ] **Shared helper** `intent._capture_and_confirm(purpose,
  deliverable, acceptance) → intent_id` is added on the `Intent`
  class. Both the CLI `intent` subcommand AND the MCP
  `intent_bootstrap` substrate tool call it — no duplication of the
  capture/confirm pair.
- [ ] **Isomorphism test** (`test_bootstrap_mcp_equals_bash_cli`):
  given the same `(purpose, deliverable, acceptance)` and a
  shared DB, the resulting Intent node carries identical props
  (status, purpose, deliverable, acceptance) regardless of surface.
  Extends the existing
  `test_isomorphism_mcp_equals_bash_cli` invariant.

## Files

```
agency/engine.py                              # +3 substrate tools in build_mcp
agency/install.py                             # +_CLAUDE_MD_SNIPPET, +write_claude_md, +agency_install_op
agency/capability.py                          # error-message rewrite (SERVES guard)
agency/capabilities/reflect.py                # docstring enum disclosure
tests/test_mcp_bootstrap.py                   # NEW — intent_bootstrap RED→GREEN
tests/test_welcome.py                         # NEW — agency_welcome shape + token budget
tests/test_install_verb.py                    # NEW — agency_install + CLAUDE.md snippet idempotency
tests/test_serves_guard_message.py            # NEW — guard message mentions intent_bootstrap
tests/test_search_empty_graph_hint.py         # NEW — empty-graph search preamble
tests/test_reflect_scope_enum_in_doc.py       # NEW — docstring carries enum domain
skills/help/SKILL.md                          # regen via install.py
```

## Open Questions

- **OQ-1 — Surface-layer naming convention (Fowler).** Should
  `agency_install` / `agency_welcome` / `intent_bootstrap` follow
  `capability_*_*` naming? They are engine-substrate, not capability
  verbs, so NO — matches `lifecycle_gate` / `memory_graph_provenance`.
  Fowler asked further: name them uniformly (`agency_*`) for the next
  maintainer's sake. Resolution: KEEP `intent_bootstrap` as-is for
  now (it reads naturally and the namespace prefix would not help an
  agent searching for "intent"); add a one-paragraph
  "Substrate tools" section to CORE.md alongside the next CORE-touch
  spec. Tracked as a follow-up; non-blocking for this implementation.

- **OQ-2 — CLAUDE.md write policy.** What if the target repo has a
  CLAUDE.md WITHOUT the agency marker? Resolution: append under the
  `<!-- agency: onboarding -->` marker, never modify existing
  content. If the marker already exists, REPLACE the block between
  markers (so re-running picks up updated guidance).

- **OQ-3 — Token budget for welcome.** [RESOLVED — names only].
  Initial draft returned `{capability: [verb, ...]}`, but the live
  registry's Jules capability alone has 21 verbs and the full-map
  payload landed at 1.2 KB. Resolution: welcome carries a SORTED
  list of capability NAMES only (≈ 100 B for the list); verbs reach
  the caller via `capability_plugin_help` (full map, ~ 600 B) or
  `search('<keyword>')` (filtered, ~ 200 B). The agent pays the verb
  cost only when it needs verbs — most first calls just need to know
  what capabilities exist for the next `search`. Net token saving:
  ≈ 800 B per first contact vs. full-map welcome.

- **OQ-4 — Mode-B vs local-dev for agency_install.** Mode B
  (`CLAUDE_PROJECT_DIR` set) writes into the user's project. Local
  dev (no env) writes into cwd. Same precedence as
  `install._scaffold_target`. Resolution: re-use that helper directly.

## Evidence

- Fehlerbericht F1, F4, F6 — KP repo `Plan/AGENCY-PLUGIN-FEHLERBERICHT.md`
  (Hoch/Mittel/Niedrig respectively).
- Session reflection — KP repo `Plan/SESSION-REFLECTION.md`
  ("Vortäuschen von Fortschritt unter Reibung" — the friction this
  spec closes).
- Spec 020 — central graph DB resolution; this spec REUSES
  `_scaffold_target` + `scaffold_db` without re-implementing them.
- Spec 023 — adaptive disclosure; this spec extends the welcome
  payload's discovery surface to follow the same brief-slice budget
  per verb.

## Failure modes & operational invariants (Nygard)

- **Non-writable target.** `agency_install` on a target whose
  filesystem rejects writes (read-only mount, missing perms): raises
  the OS-level `PermissionError`/`OSError`. FastMCP surfaces it as a
  tool result with `is_error=true` and the OS message; no partial
  state is left behind (we create `.agency/` BEFORE the
  `.gitattributes`/CLAUDE.md writes — the first failing step
  short-circuits the rest). Covered by
  `test_agency_install_non_writable_target_fails`.

- **Parallel bootstrap calls.** Two `intent_bootstrap` calls in
  parallel against the same DB are CORRECT behavior — graphqlite
  serializes the writes, and two distinct Intent nodes appear. Intents
  are not content-deduped by design (a retry IS a new intent in the
  domain model). Agents that need idempotency must check
  `memory.find('Intent', purpose=...)` before bootstrap. This is the
  same hazard as F3 (Fehlerbericht) — explicitly out of scope here,
  closed by the future Spec 031 idempotency-key work.

- **DB locked.** If the DB file is held by another process, the
  engine raises a graphqlite-level lock error on first write.
  `agency_welcome` does NOT write — so it remains callable even on a
  locked DB; the agent's recovery path is "call welcome, see db_path,
  inspect the holder, retry."

## Non-goals

- **F2 (JULES_API_KEY stale-cache).** Out of scope for this spec —
  follow-up Spec 030 covers lazy env reload + better error wording.
  The work is small but orthogonal (touches the Jules client init,
  not the bootstrap surface).
- **F3 (sandbox timeout batch idempotency).** Out of scope — needs a
  separate spec on `effect`-verb idempotency keys + streaming
  returns. Track as Spec 031.
- **F5 (system `python3` silent fail).** Documentation fix, not a
  plugin change — covered by an AGENTS.md amendment.

## Followup — Implementation Status (2026-05-31)

> Consolidation pass on branch `claude/plan-spec-review-74gHM`. Frontmatter `status:` may be stale; this section reflects verified code state.

**Verdict:** Shipped

### Done
- **§A ✓** — `intent_bootstrap` substrate tool in `agency/engine.py:217`; mints + confirms an Intent, returns `{intent_id, status, next}`, rejects empty required fields, records no Invocation node.
- **§A ✓** — `agency_install` substrate tool in `agency/engine.py:248`; wraps scaffold_db + writes CLAUDE.md snippet with idempotent marker pair (`<!-- agency:onboarding:start/end -->`); `target` defaults via `CLAUDE_PROJECT_DIR` → cwd.
- **§A ✓** — `agency_welcome` substrate tool in `agency/engine.py:346`; returns capabilities list, db_path, bootstrap_example, install_example, next steps; pure introspection (no graph writes); token budget ≤1 KB verified.
- **§B ✓** — SERVES-guard error in `capability.py:220–221` names `intent_bootstrap` and bash side-pipe.
- **§B ✗** — Empty-graph `search` preamble (`_bootstrap_hint`) NOT implemented and `tests/test_search_empty_graph_hint.py` does not exist; this was deliberately superseded by Spec 030's stateful `agency_welcome` (030 OQ-2: "Considered; rejected — stateful welcome subsumes the use case").
- **§C ✓** — `reflect.note` and `reflect.batch_note` docstrings in `agency/capabilities/reflect.py:30,44` list `Allowed scopes:` inline, sourced from `REFLECT_SCOPES`.
- **§D ✓** — `skills/help/SKILL.md` regenerated; names `intent_bootstrap`, `agency_install`, `agency_welcome` as MCP first-call surface (verified by `tests/test_install_mcp_skill.py`).
- **§E ✓** — `Intent.capture_and_confirm` shared helper; CLI and MCP paths both call it; isomorphism test `test_intent_bootstrap_mcp_equals_bash_cli` passing.

### Still to implement
- Nothing — fully shipped (§B empty-graph preamble was deliberately dropped in favour of 030 stateful welcome).

### Refinement needed (given later specs)
- Spec 030 made `agency_welcome` stateful (`fresh`/`in_progress`); the §A spec description of `agency_welcome` is now slightly stale (does not mention the `state` field or `last_intent`). The implementation is correct; only the spec text lags.
- The substrate-tools naming note (OQ-1: add "Substrate tools" section to CORE.md) is still a tracked follow-up, non-blocking.

### Evidence
- code: `agency/engine.py:209–400` (all four substrate tools); `agency/capability.py:220–221` (SERVES guard); `agency/capabilities/reflect.py:30,44` (scope enum); `agency/install.py` (CLAUDE.md snippet helpers)
- tests: `tests/test_mcp_bootstrap.py` (4 passing); `tests/test_welcome.py` (4 passing); `tests/test_install_mcp_skill.py` (4 passing); `tests/test_serves_guard_message.py` (1 passing); `tests/test_reflect_scope_enum_in_doc.py` (2 passing); `tests/test_search_empty_graph_hint.py` — absent by design
- commits/notes: `4cda286` (intent_bootstrap), `22952fe` (agency_install), `5f6358f` (agency_welcome), `9acb7f4` (SERVES guard), `897542e` (reflect scope enum); all 333 tests passing
