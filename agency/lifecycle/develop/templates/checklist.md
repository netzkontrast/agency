# $discipline — checklist

<!-- AGENT: This is the step-by-step walk for the named discipline.
Each step lists what to produce + any hard-gate the step carries.
Hard-gates pause the run at `input-required` if they fail (Spec 016
Hint #8 universal convention). -->

$steps

<!-- AGENT: After walking the checklist, record a Reflection scoped
to `observation` or `reflection` summarising what worked + what didn't.
Spec 058 lint rule enforces both SERVES and OBSERVED_DURING edges on
the Reflection node. -->
