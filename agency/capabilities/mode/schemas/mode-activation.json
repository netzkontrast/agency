{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ModeActivation",
  "description": "A recorded behavioral-mode activation (agency/capabilities/mode/_main.py). SERVES the active intent; captures the adopted session posture for provenance.",
  "type": "object",
  "required": ["mode"],
  "properties": {
    "mode": {
      "type": "string",
      "enum": ["brainstorming", "introspection", "orchestration", "task_management", "token_efficiency"],
      "description": "The activated behavioral mode."
    }
  }
}
