{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "WorldAxiom",
  "description": "An inviolable rule or tendency in a World (Spec 123). Hard axioms block the world structurally; soft axioms describe tendencies. Links to World via PART_OF_WORLD.",
  "type": "object",
  "required": ["text", "severity"],
  "properties": {
    "text": {
      "type": "string",
      "description": "The axiom statement."
    },
    "severity": {
      "type": "string",
      "enum": ["hard", "soft"],
      "description": "Hard = inviolable world rule; soft = tendency. (WORLD_AXIOM_SEVERITY enum, agency/capabilities/novel/_main.py)."
    }
  }
}
