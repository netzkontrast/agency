{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Language",
  "description": "A language spoken within a World (Spec 123). Links to World via PART_OF_WORLD.",
  "type": "object",
  "required": ["slug", "name"],
  "properties": {
    "slug": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "description": "Kebab-case unique identifier."
    },
    "name": {
      "type": "string",
      "description": "Language name."
    },
    "description": {
      "type": "string",
      "description": "Brief prose description."
    }
  }
}
