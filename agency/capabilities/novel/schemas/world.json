{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "World",
  "description": "A fictional world node in the novel sub-graph (Spec 123). Cultures, Religions, Languages, MagicSystems, and WorldAxioms hang off it via PART_OF_WORLD.",
  "type": "object",
  "required": ["slug", "name"],
  "properties": {
    "slug": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "description": "Kebab-case unique identifier for the world."
    },
    "name": {
      "type": "string",
      "description": "Human-readable world name."
    },
    "description": {
      "type": "string",
      "description": "Short prose description of the world."
    }
  }
}
