<!-- doc-source: agency/capabilities/analyze/data/review-chain.json brooks-lint@ec44ec8 skills/brooks-health/health-guide.md -->
# Quality walk — Health Dashboard

<!-- AGENT: The four-dimension chain (PR · Architecture · Debt · Test) is the
     single source review-chain.json["health"], delivered one step at a time
     (Spec 380). Render the dimensions from the chain; do NOT restate them (rule 2). -->

<!-- AGENT: This is a FAST snapshot, not a deep per-risk review — cap each
     dimension at three findings (Debt: two per risk code, three overall). Roll
     each dimension into its own score; the dashboard + the one or two
     highest-leverage actions is the deliverable, not a flat finding list. -->

<!-- AGENT: Include the mermaid dependency graph for the Architecture dimension.
     Apply the shared methodology from review-chain.json["_methodology"]. -->
