{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Reflection",
  "description": "Durable, scope-tagged cross-session memory node (Spec 060 typing for the Reflection node from reflect.py).",
  "type": "object",
  "required": ["scope", "text"],
  "properties": {
    "scope": {
      "type": "string",
      "enum": ["observation", "reflection", "project", "technical", "user", "world"],
      "description": "Lens — gates which downstream queries surface this note."
    },
    "text": {
      "type": "string",
      "description": "The reflection body."
    }
  }
}
