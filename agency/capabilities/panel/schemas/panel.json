{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Panel",
  "description": "A multi-expert panel session (agency/capabilities/panel/_main.py; Spec 299). SERVES the active intent; subject describes the topic and mode governs how experts interact.",
  "type": "object",
  "required": ["subject", "mode"],
  "properties": {
    "subject": {
      "type": "string",
      "description": "The topic or question the panel addresses."
    },
    "mode": {
      "type": "string",
      "enum": ["discussion", "debate", "socratic"],
      "description": "Interaction mode: 'discussion' (synthesis), 'debate' (adversarial), or 'socratic' (question-led)."
    }
  }
}
