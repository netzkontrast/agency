{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CommandMd",
  "description": "Required fields a slash-command markdown file must carry.",
  "type": "object",
  "required": ["name", "description", "body"],
  "properties": {
    "name": {"type": "string"},
    "description": {"type": "string"},
    "body": {"type": "string"}
  }
}
