{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Plugin",
  "description": "A Claude Code plugin manifest node (agency/capabilities/plugin/_main.py; Spec 063). Records the plugin's identity so it can be referenced in the provenance graph.",
  "type": "object",
  "required": ["name", "version", "description"],
  "properties": {
    "name": {
      "type": "string",
      "description": "Plugin package name (e.g. '@scope/plugin-name')."
    },
    "version": {
      "type": "string",
      "description": "Semantic version string (e.g. '1.2.0')."
    },
    "description": {
      "type": "string",
      "description": "Short human-readable description of the plugin's purpose."
    }
  }
}
