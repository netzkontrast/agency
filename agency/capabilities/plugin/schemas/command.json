{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Command",
  "description": "A slash command authored for a Claude Code plugin (agency/capabilities/plugin/_main.py; Spec 063). Records command identity for provenance; the full command prose lives in the command-md template.",
  "type": "object",
  "required": ["name", "description"],
  "properties": {
    "name": {
      "type": "string",
      "description": "Kebab-case command name without the leading slash (e.g. 'my-command')."
    },
    "description": {
      "type": "string",
      "description": "Short description shown in the slash-command picker."
    }
  }
}
