{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Research",
  "description": "A research run scoped to a single question (agency/capabilities/research/_main.py; Spec 044). Records the question, depth, start time, and lifecycle status. HAS_CLAIM edges link to ResearchClaim nodes after the verify phase.",
  "type": "object",
  "required": ["question", "depth", "started_at", "status"],
  "properties": {
    "question": {
      "type": "string",
      "description": "The research question being investigated."
    },
    "depth": {
      "type": "string",
      "description": "Depth tier requested (e.g. 'quick', 'standard', 'deep')."
    },
    "started_at": {
      "type": "string",
      "description": "ISO-8601 timestamp when the research run began."
    },
    "status": {
      "type": "string",
      "enum": ["planning", "fanning-out", "verifying", "ready", "superseded", "failed"],
      "description": "Lifecycle status of the research run."
    }
  }
}
