{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ResearchClaim",
  "description": "A single verified claim produced by a research run (agency/capabilities/research/_main.py; Spec 044). Linked via HAS_CLAIM←Research; text holds the claim body, research_id anchors provenance.",
  "type": "object",
  "required": ["text", "research_id"],
  "properties": {
    "text": {
      "type": "string",
      "description": "The claim body — a specific, falsifiable statement derived from research."
    },
    "research_id": {
      "type": "string",
      "description": "Node ID of the parent Research run that produced this claim."
    }
  }
}
