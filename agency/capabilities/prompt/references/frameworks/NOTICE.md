# Attribution — vendored prompt frameworks

The 27 prompt-engineering frameworks in this directory and in
`../../data/frameworks.json` are vendored from **prompt-architect** by
**ckelsoe** (https://github.com/ckelsoe/prompt-architect), licensed **MIT**
(see `LICENSE` in this directory). Per Spec 304 they are reimplemented as
graph-native agency substrate; the reference prose is preserved verbatim for
human readers. Each framework entry carries a per-entry `source_ref` naming its
upstream origin + license. Porting must not strip this provenance.
