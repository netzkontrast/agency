<!-- agency-generated: v1 -->
# mode.list

The behavioral-mode roster — name · purpose · behaviors · triggers.

## Inputs

| Param | Type | Description |
|-------|------|-------------|
| `(none).` |  |  |

## Returns

``{count, modes: [...]}``.

## Chain-next

mode.detect(context) or mode.activate(mode).

## Details

(no further detail)

## Example

```bash
agency-mode-list --intent-id $IID …
```
