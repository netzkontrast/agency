<!-- agency-generated: v1 -->
# loop.emit_runner

Write the stdlib run-loop.py (reads only loop.resolved.json) (369).

## Inputs

| Param | Type | Description |
|-------|------|-------------|
| `loop_id (str), target (str — output directory).` |  |  |

## Returns

``{file}``.

## Chain-next

run `python run-loop.py loop.resolved.json` externally.

## Details

(no further detail)

## Example

```bash
agency-loop-emit_runner --intent-id $IID …
```
