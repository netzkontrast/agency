---
description: Read the `intent` pillar — one of agency's four concepts (Intent · Capability · Lifecycle · Memory). Use when you need to understand or work with Intent — how every agency session serves a confirmed intent, the bootstrap → clarify → confirm clarity gate, and the critical-thinking methods that pressure-test an intent before work begins.
---

## `/agency-intent` — the `intent` pillar (concept)

Use when you need to understand or work with Intent — how every agency session serves a confirmed intent, the bootstrap → clarify → confirm clarity gate, and the critical-thinking methods that pressure-test an intent before work begins.

A pillar is a CONCEPT, not a walkable discipline — there are no phases to walk. Read the self-contained concept skill (it teaches `intent` with a worked example):

`skills/intent/SKILL.md`

