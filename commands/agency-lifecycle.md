---
description: Read the `lifecycle` pillar — one of agency's four concepts (Intent · Capability · Lifecycle · Memory). Use when you need to understand or work with Lifecycle — how agency models any stateful thing (a spec, a session, a branch, a Jules run) as a state machine you open, move, and close, and reads its live state with read/find/check/watch.
---

## `/agency-lifecycle` — the `lifecycle` pillar (concept)

Use when you need to understand or work with Lifecycle — how agency models any stateful thing (a spec, a session, a branch, a Jules run) as a state machine you open, move, and close, and reads its live state with read/find/check/watch.

A pillar is a CONCEPT, not a walkable discipline — there are no phases to walk. Read the self-contained concept skill (it teaches `lifecycle` with a worked example):

`skills/lifecycle/SKILL.md`

