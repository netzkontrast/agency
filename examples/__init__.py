"""Example out-of-tree extensions for the agency engine.

These are NOT part of the core plugin. They demonstrate the extension point: an
external capability loads via `Engine(..., extra_capabilities=[...])`, registering
and extending the ontology exactly like a core capability — so the core stays
minimal while extensibility is proven end to end.
"""
